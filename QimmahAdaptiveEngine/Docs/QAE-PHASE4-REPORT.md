# QAE Phase 4 Report — Real Personalization Content Integration

**Per [CTO-QAE-005].** Scope honored: legacy question oracle, bank inventory + materiality audit, content conversion, profile-evidence map, four-axis experience model, capability evidence, journeys + comparisons, docs and contracts. **No Training/Nutrition/Step/Recovery/Trends/Adaptation logic was built.** Phase-3 amendments landed (rejected answers preserve evidence — proven; registration-order independence extended to bank content; empty `provides` is a hard integrity failure; `disabledByManifest` remains reserved-only).

## The thirteen requested returns

1. **Total legacy questions inventoried: 193** — every one has a machine-readable inventory record with all mandated fields (`Contracts/content/question-inventory.json`); zero silent discards (proof-checked id-by-id).
2. **Disposition counts:** PRESERVE **138** · PRESERVE_WITH_MAPPING **15** (answer-type remaps) · REMOVE_NON_MATERIAL **34** (flagged, retained in inventory, excluded from the active bank) · PRODUCT_DECISION_REQUIRED **6** (5 unreachable clarifies L-QST-3 + `b-target-weight` L-QST-7) · SUPERSEDE/REMOVE_REDUNDANT/RESEARCH_REQUIRED **0**. Active QAE bank: **153**.
3. **Materiality findings** (PERSONALIZATION-MATERIALITY-AUDIT.md): materiality was **verified** (derivation-source reads + routing usage + cited QAE-spec consumers), not copied from `affects`. Three findings: **L-QST-7 (new)** — `b-target-weight` permanently ineligible in production (dead `answers.primaryGoal` path), so target weight never enters personalization evidence today; **queued-follow-up starvation** — the returning-user follow-ups (`x-return-reason`/`x-return-ramp`) are material but asked in zero recorded journeys under legacy *or* QAE budgets; `a-heat-sensitivity` non-material today but named in the product spec — backlog, not silently kept.
4. **Question count across the 12 scenarios:** min **16** · avg **16.0** · max **16** (soft max 16 of the founder budget 13/16/20; ≈15 target met; legacy for comparison: 17–20).
5. **Bank avoided per user:** min **88.9 %** · avg **89.4 %** · max **89.5 %** of the 153-question active bank.
6. **Beginner vs advanced:** 13 common; beginner-only `e-gym-type, g-fatloss-focus, x-confidence` (gym-literacy + goal focus + confidence probe); advanced-only `x-last-trained, x-total-duration, x-consistency` (training-history depth). Responsible evidence per question recorded in `qae-comparisons.golden.json`.
7. **Home vs gym:** 14 common; home-only `e-equipment-list` (equipment inventory — capability evidence) and `s-pregnancy` (sex-gated screening; home persona is female — adaptivity on sex evidence, recorded); gym-only deeper history questions.
8. **Injury vs none:** 13 common; injury-only `l-pain-level` (safety-clear), `l-current-areas`, `l-pain-on-movement` — the injury evidence chain; none of these asked without injury evidence.
9. **Minor vs adult:** 13 common; journeys diverge on history-vs-goal-focus questions; the minors *plan* barrier itself lives in SafetyPolicy (CRITICAL clamp) + completeness (age mandatory, unknown-age blocking, U1) — question-level goal-option masking is host/`visibleOptions` territory, recorded as an integration seam.
10. **Oracle compatibility:** the legacy oracle (12 personas, per-step records with facts/candidates/stage/score/budget/evidence/follow-ups) is **byte-identical across double runs, enforced at generation**. QAE journeys are likewise double-run + bank-permutation byte-identical. QAE↔legacy sequence relationship is **documented, not claimed as parity** (founder-changed budget + 153-vs-193 bank): identical gate/required ordering, same early sequences, QAE stops at 16 where legacy runs 17–20. Prior compatibility surfaces still green: minors matrix 15/15; the 13 plan goldens byte-identical after every change this phase.
11. **Unresolved product decisions:** (i) the 5 unreachable clarifies — reach or delete at bank freeze (U4 fate per question); (ii) **L-QST-7** — revive `b-target-weight` by mapping to `derived.goalKey` (behavior change) or retire it; (iii) **returning-user follow-up starvation** — raise priority / make conditionally-required for returning users, or accept that pair-E differentiation stays classification-level; (iv) legacy `select.min/max` multi-select constraints not yet enforced in QAE normalization (contract gap for bank freeze); (v) minor question-level option masking placement (host vs content) at integration.
12. **Full gate results:** QAE suites — Phase 1 **52/52** · Phase 2 **43/43** · Phase 3 **41/41** · Phase 4 content **26/26** (incl. positive/negative/bypass per routing class, evidence-preservation on invalid answers, envelope completeness, no-floats, scope scans). Repo — `typecheck` ✓ `lint` ✓ (max-warnings 0) `build` ✓ full `test:gate` green.
13. **Recommended first Training Engine wave:** *capability + metadata foundation first* — (a) extend the plan-oracle harness to record legacy exercise-selection outcomes per fixture (pool, picks, injury exclusions) as selection goldens; (b) define the `ExerciseMetadata` seeding from the characterized id-blocklists and machine catalog (blocklist ⊆ contraindications proven by a named check); (c) implement the U3 capability-based selection order (MovementPattern → RequiredEquipment → Constraints → Difficulty → Preference → Exercise) as declarative policy over the capability evidence this phase already produces — parity-mode against legacy for home/bodyweight paths, approved-deviation mode for L-TRN-1 (full gym). Small, attackable, and it retires the highest-risk supersession first.

## Deliverables index

Docs: QUESTION-CONTENT-MIGRATION · PERSONALIZATION-MATERIALITY-AUDIT · PROFILE-EVIDENCE-MAP · EXPERIENCE-MODEL · QUESTION-ORACLE-COVERAGE (+ this report; defect register gains L-QST-7).
Contracts: `Contracts/content/` — question-bank.qae.json (153, `bankManifestHash`) · question-inventory.json (193) · bank-config.qae.json (8 transcribed conflicts, founder budgets, gate) · profile-evidence-map.json · personas.json; migration log updated (predicate `exists/notExists`, normalization `text/openList`, `copyKey`, `budgetOverrideReason` — all golden-neutral, verified).
Fixtures: `Fixtures/golden/questions/` — 12 legacy oracle goldens + summary · 12 QAE journey goldens (full mandated envelope) · comparisons golden · summary.
Runtime added: `Domain/ProfileClassification/classify.ts` only — classification for routing/completeness; no plan generation anywhere (proof-scanned).

## Stop

Phase 4 complete. Halting before any Training/Nutrition/Step/Recovery/Trends/Adaptation work, per §15.

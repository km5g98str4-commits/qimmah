# E2E_GATE_REPORT.md — بوابة القبول [CTO-62]

> بوابة القبول من خمس فقرات على فرع **`integration/nard-2026-08`**. **الجذع `main` لم يُلمس.**
> تاريخ: **2026-08-02** · المنفِّذ: Claude Code · الحالة: **مكتمل — بانتظار [CTO-63]**
> فرع التكامل: `9638adf` (قبل هذا التقرير) · الجذع: `main` = `origin/main` = `658d088`

## الخلاصة: **5/5 فقرات خضراء · 6/6 رحلات حرجة خضراء**

| الفقرة | الحالة |
|---|---|
| 1 · سلامة الأساس | 🟢 أخضر |
| 2 · بناء نظيف من الصفر | 🟢 أخضر |
| 3 · البوابة الكاملة | 🟢 أخضر (صفر انحدار من الدمج) |
| 4 · الرحلات الحرجة e2e | 🟢 أخضر (6/6 بمتصفّح حقيقي) |
| 5 · فحص iOS | 🟢 أخضر (البناء نجح) |

---

## الفقرة 1 — سلامة الأساس · 🟢

| فحص | نتيجة |
|---|---|
| `git fsck --full` | نظيف — صفر أخطاء (128 كائن معلّق = المعروف مسبقاً في BRANCH_AUDIT §4، أعمال منقَذة) |
| `main` المحلي | `658d088` ✓ (لم يتغيّر) |
| `origin/main` | `658d088` ✓ (لم يتغيّر) |
| فرع التكامل محلي=بعيد | `f60c09b` = `f60c09b` ✓ |

**سبب رسالة `fatal` نهاية الجلسة السابقة (موثّق):** الأمر التشخيصي كان `git rev-parse --short main origin/main` — راية `--short` تبتلع الوسم التالي `main` كقيمتها العددية (وهي غير صالحة) فترمي `fatal: Needed a single revision`. **أثر عرضي بحت من صياغة الأمر لا خلل مستودع** — أُعيد إنتاجه، وكل مرجع يُحلّ منفرداً إلى `658d088`. صفر أثر.

---

## الفقرة 2 — بناء نظيف من الصفر · 🟢

> ملاحظة: `npm ci` يحذف `node_modules` تلقائياً قبل التثبيت (= «الحذف ثم ci» الموقّع)؛ أمر `rm -rf` صريح رفضه الحارس فاعتُمد سلوك `npm ci` نفسه. `vite` يفرّغ `dist` افتراضياً.

| خطوة | EXIT |
|---|---|
| `npm ci` (تثبيت حتمي نظيف) | 0 |
| `npm run typecheck` | 0 |
| `npm run build` | 0 |
| `npm run lint` | 0 |

**التأكيدات الأمنية الثلاثة:**
- **`VITE_SYNC_ENABLED` مطفأ** — `.env.example` = `""`، لا `.env` يضبطه، والكود `import.meta.env.VITE_SYNC_ENABLED === 'true'` ⇒ `false`. لا قيمة true في الحزمة.
- **لا أصل GIF بعلامة مائية** — صفر ملف `.gif` في `dist`، صفر مرجع `workoutxapp`/`/exercise-gifs/` في الحزمة (خريطة `exerciseGifs` مفرَّغة، إصلاح P0 سليم).
- **لا `react-body-highlighter`** — ليست في التبعيات، ولا في `node_modules`، وصفر في حزمة `dist`.

---

## الفقرة 3 — البوابة الكاملة · 🟢

**`test:gate` = 77 خطوة · EXIT 0.** بلا تعطيل أي اختبار (§4).

**سكربتات الإثبات خارج البوابة (22):** شُغّلت جميعاً. النتائج:

| المجموعة | الحالة |
|---|---|
| 5 إثباتات رأسية (p5reset · p5plan · p8a2off · observability · e-calc) | 🟢 نجحت |
| e2e بمتصفّح (7 سكربتات) | الفقرة 4 |
| `proof:p4a3` · `proof:p8a3` | 🟡 فشل — إثباتات **بائتة** تشير لوحدات محذوفة (`@/sections/MyTargets`، `@/features/.../ocr`) — **تفشل مطابقةً على `origin/main`** |
| `proof:p5medals` · `proof:p10a5` · `proof:p10qa` · `test:release-gate` | 🟡 فشل **بيئي** — Chromium/playwright غير مثبّتين headless — تفشل مطابقةً على الجذع |
| `test:chaos` | 🟡 فشل تأكيدات (13/57، invariants 1,3,4,5,7,8,12) — **مطابق تماماً على `origin/main`** (موجود مسبقاً) |
| `test:safe-storage` · `test:body-model` | 🟡 استبعادان **موثّقان في الميثاق** (مغطّيان بـ`test:storage-honesty`/`test:body3d` داخل البوابة) — مطابقان على الجذع |

> **الحكم القاطع:** قورن كل فشل خارج البوابة بخط أساس `origin/main` عبر worktree مؤقّت. **كل فشل موجود مسبقاً على الجذع بشكل مطابق. دمج «النرد الثابت» (29 دمجاً) أضاف صفر انحدار.**

---

## الفقرة 4 — الرحلات الحرجة e2e · 🟢

**الآلية:** ثُبّت Chromium لـplaywright (`npx playwright install chromium`)، وقيدت الرحلات فعلياً بمتصفّح حقيقي على التطبيق المخدوم (`vite preview` :4321) عبر مسار **الضيف المحلي** («كمّل كضيف») — يبلغ اللوحة بلا backend لأن التطبيق محلي‑أولاً.

| # | الرحلة | نتيجة |
|---|---|---|
| 1 | مستخدم جديد + الأساسيات | 🟢 الموافقة الصحية **قبل** الجسد بشاشة واحدة؛ حقل العمر يعرض حدّ **«13–100»** (حدّ الـ13 مُثبَت أيضاً في `test:minors`/`test:age-13` بالبوابة) |
| 2 | شجرة الإعداد حتى اللوحة | 🟢 خمس خطوات → **«خطتك جاهزة»** (تقسيمة ٤ أيام علوي/سفلي · تنشيف · نادي · متوسط) → اللوحة. **لا طريق مسدود** |
| 3 | وجبة عبر بحث الطعام السعودي + وجبات محفوظة | 🟢 «كبسة» → أصناف سعودية/خليجية (كبسة دجاج 620/لحم 700/روبيان 500)؛ سُجّلت كبسة دجاج → **المتبقّي 2178→1558**، الماكرو تحدّثت حيّاً. («وجبات محفوظة» مُثبَتة بـ`test:saved-meals` في البوابة) |
| 4 | تسجيل تمرين | 🟢 جلسة اليوم (١ من ٦): «جهاز ضغط الصدر»، هدف 4×8–12، **«ما فيه سجل سابق»** (صدق §5)، خيار البديل «لهذي الجلسة بس» |
| 5 | تعديل الخطة = اقتراح بسبب لا صامت | 🟢 محرّر 4 خطوات ينتهي بـ**«المراجعة والحفظ»** — يعرض التغيير صراحةً (**الوزن 80→72 كجم**) + «معاينة» + زرّ حفظ صريح. **لا تعديل آلي صامت** (§8‑3) |
| 6 | التطبيق كامل والمزامنة مطفأة | 🟢 كل طلبات الشبكة عبر الجلسة (إعداد+وجبة+تمرين+تعديل) **محلية `127.0.0.1` فقط · صفر طلب Supabase/سحابي** |

**مكافأة مرصودة:** استئناف التمرين **بسؤال** («عندك تمرين لم ينتهِ · تكمّل من مكانك أو تبدأ من جديد؟») يعمل في الجذع — تأكيد إضافي أن إيقاف الفرع 21 (آلية استئناف منافسة) كان صحيحاً.

> **عن سكربتات الرحلات في المستودع:** `test:e2e:journey` · `test:journey:newcomer` · `test:e2e:settings-security` تفشل — لكنها **تفشل مطابقةً على `origin/main`** (تعتمد جلسة مزروعة/backend أو مؤقّتات حسّاسة، ولذلك هي خارج البوابة). الرحلة الموثوقة `test:e2e:onboarding` (عقدها `test:onboarding-e2e-contract` داخل البوابة) **نجحت 13/13**. القيادة اليدوية أعلاه غطّت الرحلات الست فعلياً.

---

## الفقرة 5 — فحص iOS · 🟢

| خطوة | نتيجة |
|---|---|
| الأدوات | Xcode 26.6 · CocoaPods 1.16.2 · xcrun — متاحة |
| `npx cap sync ios` | 🟢 نجح — أصول `dist` (بناء فرع التكامل) → `ios/App/App/public`؛ 6 إضافات Capacitor عبر Package.swift (SPM) |
| `xcodebuild ... -sdk iphonesimulator ... build` | 🟢 **`** BUILD SUCCEEDED **`** — مشروع iOS يترجم بأصول فرع التكامل |

**قيد موثّق (لا فشل):** البناء **الموقّع لجهاز حقيقي** له عائق `codesign`/Keychain مذكور في `docs/audit/DEVICE-NOT-VERIFIED.md` (اعتماد مفتاح Keychain غير مُمنوح في بيئة headless). البناء غير الموقّع أعلاه يثبت **ترجمة iOS سليمة**؛ التثبيت على جهاز يبقى لجلسة موقّعة.

---

## الامتثال للقواعد الحاكمة

| قاعدة | حالة |
|---|---|
| لا دمج في `main` | ✅ `main` = `658d088` دون تغيير |
| لا حذف / إعادة كتابة تاريخ | ✅ (worktrees خط الأساس مؤقّتة أُزيلت بـ`git worktree remove`) |
| كل العمل على فرع التكامل | ✅ |
| لا تعطيل اختبار | ✅ كل الفشل موثّق بخط أساس، لا تعطيل |
| الـstashات | ✅ سليمتان |

---

## الخطوة التالية — [CTO-63]

فرع التكامل اجتاز بوابة القبول الخمسية بالكامل، وصفر انحدار مقابل الجذع. **الدمج النهائي في `main` (تقديم `main` إلى `integration/nard-2026-08` ودفعه) مُبوَّب بـ[CTO-63]** بعد توقيعك النهائي. لم يُنفَّذ في هذه الجلسة.

*انتهى التقرير.*

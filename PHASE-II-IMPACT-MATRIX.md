# PHASE-II-IMPACT-MATRIX — أثر تغييرات Codex على حارات المرحلة الثانية

**الأمر:** `[QIM-CODEX-WORK-TRACE-001]` البند ١٤ · **قراءة وتحليل فقط. لم يُنفَّذ أي merge أو rebase أو cherry-pick (البند ١٥).**

| | |
|---|---|
| **الأساس المشترك لكل الحارات** | `1bcf7a9` = `[PKG-7][green]` — **مُتحقَّق بـ`git merge-base` لكل فرع على حدة** |
| **الرأس الذي يجب الالتحاق به** | `d83add2` (بعده PKG-8 · PKG-9 · docs) |
| **الفارق المطلوب استيعابه** | ٣ التزامات · ٩ ملفات مصدر |

---

## 0. النتيجة قبل التفصيل

**التعارض المتوقّع محسوب لا مقدَّر** — بـ`git merge-tree --write-tree` (عملية قراءة بحتة، لا تلمس الشجرة ولا تنشئ التزامًا):

| الحارة | جديد / معدَّل | ملفات مشتركة مع PKG-8/9 | تعارضات متوقّعة | متأثّرة؟ |
|---|---|---|---|---|
| **Release Convergence** | 16 A / 1 M | لا شيء | **صفر** | ⚠ نعم — **بازدواج، لا بتعارض** |
| **Coordinator seam** (postweb) | 4 A / 0 M | لا شيء | **صفر** | ⚠ نعم — **بوثيقة بائتة** |
| **Food** | 34 A / 2 M | `package.json` | **١** — `package.json` | ✅ نعم — سطر واحد |
| **Executive Dashboard** | 27 A / 1 M | `package.json` | **١** — `package.json` | ✅ نعم — سطر واحد + عقدان |
| **Exercise** | 20 A / 3 M | `package.json` | **١** — `package.json` | ✅ نعم — سطر واحد |

> **لماذا الرقم بهذا الصغر:** الحارات الخمس **إضافية بالكامل تقريبًا** — **١٠١ ملف جديد** مقابل
> **٧ ملفات معدَّلة فقط** عبر الحارات كلها (`package.json` ×3 · `.gitignore` ×2 · `src/lib/coaching/cues.ts` · `src/lib/coaching/index.ts`).
> ولا واحد من الملفات التسعة التي غيّرها PKG-8/9 يقع في مسار أي حارة.
> **وهذا لم يكن صدفة** — `HEAD-DEPENDENCIES.md` نصّ على القاعدة صراحةً: «ما يلامس ملفاته يُسجَّل ولا يُنفَّذ.»

---

## 1. Release Convergence — `codex/qimmah-release-convergence-001` @ `4b570c9`

| | |
|---|---|
| **متأثّرة بتغييرات Codex؟** | ⚠ **نعم — لكن ليس بتعارض دمج** |
| **الالتزامات** | ٤ فوق PKG-7: `61933b0` · `59dae0f` · `a9bfba0` · `4b570c9` |
| **الالتزامات المؤثّرة عليها** | `9f88e43` (PKG-9) — **ازدواج**، لا تعارض |
| **الملفات/العقود** | `scripts/release/**` (١٤ ملفًا جديدًا) · `docs/execution/qimmah-postweb/release/**` · `.gitignore` |
| **التعارض المحسوب** | **صفر** |
| **المخاطرة** | 🟠 **متوسطة** — ازدواج تنفيذ يخالف §2 |

### ⚠ DUP-1 — تنفيذان مستقلّان لسلامة الأصل المبني

| | PKG-9 | حارة الإطلاق |
|---|---|---|
| الملف | `scripts/production-bundle-safety-proof.mjs` | `scripts/release/static/bundle-safety.mjs` |
| المشغِّل | `npm run test:bundle-safety` (٩/٩) | داخل حصّاد التقارب |
| النطاق | بذرة الاستحقاق · نقاط نهاية تطويرية | **أوسع**: + خرائط المصدر · ربط Salla · تطابق المسارات/إعادة الكتابة |
| القوّة | **أقوى تصميمًا** — بناءان: إنتاج (البذرة غائبة) + mock (البذرة حاضرة) ⇒ **يُثبت أن الماسح مبصر** | بناء واحد |

### ⚠ DUP-2 — تنفيذان مستقلّان للحالة التالفة

| | PKG-9 | حارة الإطلاق |
|---|---|---|
| الملف | `scripts/e2e/dirty-state-recovery.mjs` (٤٧ تأكيدًا) | `scripts/release/personas/p5-dirty-state.mjs` |
| التغطية | **١١ حالة مزروعة** + إقلاع بكتابة محجوبة | نواقل، كلٌّ في سياق جديد مستقلّ |
| ميزة فريدة | حالة `completed-not-boolean` — **هي التي كشفت BUG-028** | عزل الأعطال: فشل لا يُقنّع فشلًا |

**السبب البنيوي:** الحارة تفرّعت من PKG-7 و**لا تستطيع رؤية** PKG-9 (أُودع بعدها). فهذا **ليس إهمالًا** —
بل النتيجة الحتمية لتوازٍ بلا سجلّ تبعيات محدَّث. وهو بالضبط ما يحذّر منه §2:
> *«فرع تخلّف ٤٢٦ التزامًا كاد يعيد بناء الترجمة كاملة وهي منجزة.»*

**لا واحد منهما مجموعة جزئية من الآخر** — فالحسم ليس «احذف أحدهما».

### التكيّف المطلوب
1. **قرار ملكية من المؤسس** (§7 — التعارض يُرفَع ولا يُحسم اجتهادًا):
   إمّا (أ) الحصّاد يستدعي حارسَي PKG-9 بدل نسختيه، وإمّا (ب) يبقى الاثنان بحدود مُعلَنة في رأس كل ملف.
2. **قبل أي دمج:** يُنقل ما في كل تنفيذ من تأكيدات **سلوكية** لا نظير لها عند الآخر
   (نمط «الإثباتان خارج البوابة» في الميثاق §11 — **التعطيل الممنوع هو الصامت**).

**استراتيجية الهجرة الموصى بها:** `merge` أمامي مباشر (صفر تعارض)، **ثم** موجة حسم ازدواج منفصلة.

---

## 2. Coordinator seam — `codex/qimmah-postweb-convergence-001` @ `c0e337f`

| | |
|---|---|
| **متأثّرة بتغييرات Codex؟** | ⚠ **نعم — وهي الأكثر تأثّرًا فعليًا** |
| **الالتزامات** | ٤ فوق PKG-7 — **وثائق حصرًا** |
| **الالتزامات المؤثّرة** | `e8f3bb6` · `9f88e43` · `d83add2` — **الثلاثة** |
| **التعارض المحسوب** | **صفر** (لا كود) |
| **المخاطرة** | 🔴 **عالية — ليست تقنية بل معلوماتية** |

### ⚠ DIV-2 — سجلّ التبعيات متأخّر ٣ التزامات

`docs/execution/qimmah-postweb/HEAD-DEPENDENCIES.md` يقول اليوم:

| ما تقوله الوثيقة | ما يقوله Git |
|---|---|
| «آخر checkpoint مرصود: `1bcf7a9` = `[PKG-7][green]`» | آخر checkpoint هو **`d83add2`**؛ وPKG-8 وPKG-9 أُودعا ودُفعا |
| «ما تبقّى في مشواره: **Layer 3 (Profile)** · Layer 4 · Layer 6» | **Layer 3 Profile منجَز في PKG-8**؛ والبرنامج أعلن **بوابته النهائية** عند `9f88e43` |
| «المراقب فعّال — ينبّه عند كل checkpoint» | لم يُسجَّل تنبيه لـPKG-8/9 في الوثيقة |

**لماذا هذا خطر وليس تفصيلًا:** هذه الوثيقة هي **ما يقود لحظة فتح البوّابة**. ومنسّق يقرؤها
سيخطّط لانتظار Layer 3 وقد انتهى، وسيبني على PKG-7 وقد تجاوزه المشوار ثلاث خطوات.

### بنود تبعية يجب إعادة تقييمها مقابل الرأس الحقيقي

| البند | ما تقوله الوثيقة | الحقيقة على `d83add2` |
|---|---|---|
| **D-01/D-02** | ترحيل `src/features/products/store.ts` — «ملف حيّ في سطح التغذية الذي يعدّله المشوار» | ✅ **لم يمسّه المشوار إطلاقًا** — التبعية أضعف مما سُجِّل |
| **D-03** | تحويل `src/data/foodItems.ts` إلى الطبقة الساخنة | ✅ لم يُمسّ — صالح كما كُتب |
| **D-04** | «`PKG-6` يملك سطح المكتبة الآن» | ✅ **دقيق** — `exerciseLibrary.ts` مستقرّ منذ PKG-6 |
| **D-05** | ربط واجهة البحث بالفهرس | ✅ لم يُمسّ |
| **E-2** | الوصل: `appRoutes.ts` + `App.tsx` + مضيف حالة | ⚠ **`App.tsx` تغيّر في PKG-9** — الخطّة صالحة، والهدف تحرّك |

### التكيّف المطلوب
1. **تحديث `HEAD-DEPENDENCIES.md` إلى `d83add2`** — **شرط قاطع قبل استئناف Phase II.**
2. تسجيل **DUP-1/DUP-2** و**RISK-1/RISK-2** (من `CODEX-WORK-TRACE.md`) في نفس السجلّ.
3. إضافة **بند تبعية جديد `D-06`:** «أي وصول خام لمفتاح `qimmah:quick-log-intent` محظور —
   المالك القانوني `src/lib/quickLogIntent.ts` منذ PKG-9.»

**استراتيجية الهجرة:** لا هجرة كود. **تحديث وثيقة فورًا** — أرخص بند في المصفوفة كلها وأعلاها عائدًا.

---

## 3. Food — `codex/qimmah-food-production-001` @ `460d538`

| | |
|---|---|
| **متأثّرة؟** | ✅ **نعم — سطر واحد** |
| **الالتزامات** | ١٢ فوق PKG-7 |
| **الالتزامات المؤثّرة** | `9f88e43` (`package.json`) |
| **الملفات المشتركة** | `package.json` **فقط** |
| **التعارض المحسوب** | **١** — `CONFLICT (content) in package.json` |
| **المخاطرة** | 🟢 **منخفضة** |

**العقود التي تلمسها:** `src/lib/food/**` (جديد) · `src/lib/text/foodNormalize.ts` (جديد) ·
`data/food-production/**` (جديد) · ٦ سكربتات npm جديدة (`food:*`, `test:food-production`).
**لا شيء منها في مسار PKG-8/9.**

**سبب التعارض بدقّة:** الحارة تُلحق `&& npm run test:food-production` **بعد** `test:workout-day-source`،
وPKG-9 أدرج `&& npm run test:quick-log` **قبلها** — نفس السطر، تعديلان.

### التكيّف المطلوب
1. **حسم `package.json` بالاتحاد** — يجب أن يبقى **كلا** السكربتين.
2. ⚠ **§4.1 إلزامي:** بعد الحسم **تُطبع قيمة `test:gate` ويُتحقَّق منها**، لا يُفترض النجاح.
   ```bash
   node -e "console.log(require('./package.json').scripts['test:gate'])" | tr '&' '\n' | grep -c 'npm run'
   ```
   *سابقة الميثاق:* دمج `release/v1.2.0-rc` حسم `package.json` بـ`--theirs` فأعاد `license: "Commercial"`
   وألغى تصحيح هوية دُمج قبله بدقائق. **التُقط بطباعة القيمة بعد الحسم.**
3. **B-2 يبقى بندًا مفتوحًا مستقلًّا:** نسب مصدر ODbL في الواجهة — **التزام ترخيص لا تحسين**،
   وهو من مسؤولية الحارة لا من أثر Codex.
4. **جديد بعد PKG-9:** أي بيانات تُشحن تمرّ الآن بـ`test:bundle-safety` على الأصل المبني.
   يُنصح بتشغيله بعد ابتلاع الطبقة الساخنة (حجم الأصل ومحتواه يتغيّران).

**استراتيجية الهجرة:** `merge` أمامي · حسم `package.json` بالاتحاد · طباعة القيمة · `npm ci` · البوابة الكاملة.

---

## 4. Executive Dashboard — `codex/qimmah-executive-dashboard-001` @ `bed2b66`

| | |
|---|---|
| **متأثّرة؟** | ✅ **نعم — سطر واحد الآن، وعقدان عند الوصل** |
| **الالتزامات** | ٥ فوق PKG-7 |
| **الالتزامات المؤثّرة** | `9f88e43` (`package.json`) الآن · `e8f3bb6` + `9f88e43` عند الوصل |
| **الملفات المشتركة** | `package.json` **فقط** |
| **التعارض المحسوب** | **١** — `package.json` |
| **المخاطرة** | 🟡 **منخفضة الآن · متوسطة عند الوصل** |

`src/admin/**` (١٦ ملفًا) **وحدة مستقلّة بذاتها** — لا تلمس `App.tsx` ولا `appRoutes.ts` بعد.
ولهذا تعارضها اليوم سطر واحد. **لكن خطّتها المعلنة (E-2) تستهدف بالضبط ما تحرّك:**

### ⚠ عقدان يجب أن يتكيّف معهما الوصل

| # | العقد | المصدر | الأثر على اللوحة |
|---|---|---|---|
| **C-7** | **`h1` واحد يملكه `MobileShell` للمسار** | PKG-8 / BUG-022 | `AdminShell` **خارج `MobileShell`** (بنصّ E-2). فمن يملك `h1`؟ **قرار صريح مطلوب** — والحارس `test:e2e:profile` **خارج CI** فلن يلتقط الخطأ آليًا |
| **C-10** | `formatNumber` حدّ عرض وحيد؛ العربية بأرقام عربية | PKG-7 | لوحة **مقاييس** = أرقام كثيرة. تجاوز الحدّ يخالف عقدًا قائمًا ويُسقط `test:settings-preferences` |

**وعقد ثالث لم يتغيّر لكنه حاكم:** طبقة `src/lib/access/**` هي **السلطة الوحيدة**.
حارس الدور الجديد (`src/admin/auth/adminRole.ts`) يجب **ألا يفرّع** سلطة موازية —
«فحوص الواجهة وحدها غير كافية؛ query/localStorage ليست سلطة».

**وE-1 قرار مؤسس معلَّق** (مطالبة JWT أم جدول `admin_users`) — **ليس أثرًا من Codex**، لكنه يسبق الوصل.

### التكيّف المطلوب
1. حسم `package.json` بالاتحاد (٤ سكربتات `test:admin-*`) + طباعة القيمة.
2. **قبل الوصل:** حسم ملكية `h1` لـ`AdminShell` صراحةً، وتمرير كل رقم معروض عبر `formatNumber`.
3. **موجة الوصل مستقلّة** — E-2 يصفها بـ«ثلاثة تعديلات لا أكثر»، وهي **موجة منفصلة عن الهبوط**.

**استراتيجية الهجرة:** هبوط الوحدة المستقلّة أولًا (رخيص، معزول)، **ثم** موجة وصل بقرار المؤسس على E-1.

---

## 5. Exercise — `codex/qimmah-exercise-production-001` @ `e76ad11`

| | |
|---|---|
| **متأثّرة؟** | ✅ **نعم — سطر واحد** |
| **الالتزامات** | ٤ فوق PKG-7 |
| **الالتزامات المؤثّرة** | `9f88e43` (`package.json`) |
| **الملفات المشتركة** | `package.json` **فقط** |
| **التعارض المحسوب** | **١** — `package.json` |
| **المخاطرة** | 🟢 **منخفضة** |

الحارة تعدّل ملفَّي مصدر: `src/lib/coaching/cues.ts` و`src/lib/coaching/index.ts` —
**ولم يمسّهما PKG-8/9 ولا حتى PKG-6.** والباقي جديد (سجلّ فيديو · مانيفست · إرشاد إنجليزي لـ١٨١ تمرينًا).

**العلاقة الحقيقية مع PKG-6 (وهي داخل القاعدة لا بعدها):**
`ExerciseDetail.tsx` و`ExerciseLibraryView.tsx` و`exerciseLibrary.ts` استقرّت في PKG-6،
فبنود الوصل C-1…C-5 في سجلّ التبعيات **ما زالت صالحة حرفيًا** — الملفات لم تتحرّك بعدها.

### التكيّف المطلوب
1. حسم `package.json` بالاتحاد (٥ سكربتات) + طباعة القيمة.
2. **⚠ C-5 من سجلّ التبعيات:** بعد وصل `getCue(ex.id)` → `getCue(ex.id, lang)`،
   يصير **وصف** فحص في `test:guidance-honesty` غير مطابق. **يُعاد عنونته بنصّه الجديد.**
   وهذا **تشديد لا تليين** — الفحص يبقى، والاسم يصير صادقًا.
3. **C-4 قرار مؤسس معلَّق** (نبرة عنواني ATHLEAN-X) — ليس أثرًا من Codex.

**استراتيجية الهجرة:** `merge` أمامي · حسم `package.json` بالاتحاد · `npm ci` · البوابة الكاملة.

---

## 6. مصفوفة الهبوط المجمَّعة

**الميثاق §1.4/٣: التوازي في البناء، والتسلسل في الهبوط.** بعد كل هبوط تعيد بقية الحارات
التأسيس + `npm ci` + البوابات قبل PRها.

| الترتيب | الحارة | لماذا هنا | كلفة |
|---|---|---|---|
| **٠** | **تحديث `HEAD-DEPENDENCIES.md`** | لا كود، وهو **ما يقود كل قرار بعده** | دقائق — **ابدأ به** |
| **١** | Coordinator seam (postweb) | وثائق حصرًا · صفر تعارض | صفر |
| **٢** | Release Convergence | صفر تعارض · ويعطي حصّادًا يخدم من بعده | صفر (+ ازدواج مؤجَّل) |
| **٣** | Exercise | سطر واحد · لا يلمس أحد | منخفضة |
| **٤** | Food | سطر واحد · أثقل بيانات ⇒ يستفيد من حصّاد سبقه | منخفضة |
| **٥** | Executive Dashboard | معزول الآن؛ وصله موجة مستقلّة بقرار مؤسس معلَّق | منخفضة الآن |

> **لماذا الترتيب لا يغيّر الكلفة هنا:** كل صاحب تعارض يتعارض في **`package.json` وحده** ومع
> **الرأس** لا مع أخيه. فالترتيب اختيار انضباط لا اقتصاد — بخلاف تصادم المجسّم 3D حيث حدّد **من يصير صاحب الأرض**.

### إجراء ثابت بعد كل هبوط
```bash
npm ci                                   # حتمًا — لا npm install (§4)
node -e "console.log(require('./package.json').scripts['test:gate'])"   # §4.1 — اطبع وتحقّق
npm run typecheck && npm run lint && npm run build && npm run test:gate
gh pr checks <رقم> ; gh run list --branch main --limit 5                # §4.0 — اقرأ CI
```

---

## 7. المخاطر المجمَّعة

| # | المخاطرة | الحارة | الشدّة | التخفيف |
|---|---|---|---|---|
| R-1 | `HEAD-DEPENDENCIES.md` متأخّر ٣ التزامات | Coordinator | 🔴 عالية | تحديث فوري إلى `d83add2` |
| R-2 | ازدواج سلامة الأصل (DUP-1) | Release | 🟠 متوسطة | قرار ملكية من المؤسس؛ نقل التأكيدات الفريدة قبل أي حذف |
| R-3 | ازدواج الحالة التالفة (DUP-2) | Release | 🟠 متوسطة | كما أعلاه — **ولا واحد مجموعة جزئية من الآخر** |
| R-4 | حسم `package.json` بـ`--theirs`/`--ours` يبتلع سكربتات | Food · Admin · Exercise | 🟠 متوسطة | اتحاد + **طباعة القيمة** (§4.1) |
| R-5 | ملكية `h1` عند وصل `AdminShell` | Dashboard | 🟡 منخفضة الآن | قرار صريح قبل الوصل؛ وحارسه خارج CI (R-7) |
| R-6 | أرقام اللوحة تتجاوز `formatNumber` | Dashboard | 🟡 منخفضة | تمرير كل رقم معروض عبر الحدّ الواحد |
| R-7 | حرّاس PKG-8/9 الأقوى **خارج `test:gate` وخارج CI** | الكل | 🟠 متوسطة | تشغيل يدوي مُعلَن عند كل هبوط، أو موجة ربط بالبوابة |
| R-8 | حارس استثناء «لغة القالب» وُسِّع (RISK-1) | الكل | 🟢 منخفضة | ربط كل عبارة مشروعة بسطحها المالك |
| R-9 | **لا دليل Safari/WebKit لأي نتيجة** | الكل | 🔴 عالية | تمرير iOS Safari على الأصل المبني — **يسبق أي إطلاق** |

> **R-9 هي الأعلى أثرًا خارج نطاق هذا الأمر.** BUG-024 عطل **بشكل Safari** أُثبت إصلاحه على مستوى
> الوحدة فقط، والجمهور سعودي/خليجي على الجوال حيث iOS Safari مهيمن.

---

## 8. الخلاصة

```
CODEX_TRACE_COMPLETE          = YES
PHASE_II_BASELINE_UNDERSTOOD  = YES
SAFE_TO_RESUME_PHASE_II       = YES (بشرطين إجرائيين، لا كود فيهما)
```

**الشرط ١ — تحديث `HEAD-DEPENDENCIES.md` إلى `d83add2`.** سجلّ التبعيات هو ما يقود لحظة الهبوط،
وهو اليوم يصف Layer 3 على أنه «متبقٍّ» وقد أُنجز في PKG-8.

**الشرط ٢ — قرار المؤسس على DUP-1/DUP-2.** ازدواج تنفيذ في حارس جودة قرار ملكية، لا اجتهاد وكيل (§7).

**والباقي عمل اعتيادي:** ثلاثة حسومات في `package.json` بالاتحاد، مع طباعة القيمة بعد كل حسم.

---

**لم تُنفَّذ أي هجرة ولا rebase ولا cherry-pick.** كل ما ورد أعلاه من `git log/show/diff/merge-tree --write-tree`
ومن المصدر الفعلي — و`merge-tree` عملية قراءة لا تلمس الشجرة ولا تنشئ التزامًا.

*المصاحبان:* [`CODEX-WORK-TRACE.md`](./CODEX-WORK-TRACE.md) · [`CONTRACT-CHANGES-SINCE-PKG7.md`](./CONTRACT-CHANGES-SINCE-PKG7.md)

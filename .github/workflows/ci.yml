name: CI

# Runs the repo's TRUE quality gate on every push and PR to any branch.
# Mirrors the local gate exactly: typecheck -> lint (0 warnings) -> production
# build -> perf budget -> the repository's canonical deterministic test gate.

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

# One in-flight run per ref; new pushes cancel the previous run to keep it fast.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  gate:
    name: Quality gate (typecheck · lint · build · proofs)
    runs-on: ubuntu-latest
    # [COMMISSIONING] ٢٠ ⇐ ٣٥ دقيقة. **ليس تليينًا لحدٍّ ضاق، بل مطابقةً لعملٍ
    # اتّسع:** أضافت هذه الموجة ثلاث خطوات (إقلاع PostgreSQL · رحلات التكليف
    # على قاعدة حقيقية · اتّصال الجلسة بمتصفّح حيّ)، فصار السير يلامس السقف
    # بلا هامش — ‏19:55 ثم 20:28، ثم أُلغي أحدها عند 20:21 **في الخطوة الأخيرة
    # وحدها** بعد نجاح عشرين خطوة قبلها.
    #
    # وسيرٌ يعيش على ١٠٠٪ من ميزانيته يسقط عشوائيًّا إلى الأبد، فيُقرأ الأحمر
    # ضجيجًا ويُطبَّع — وهو ما يمنعه §4.0 نصًّا. والهامش يُعطى بصراحة ويُعلَّل،
    # ولا تُحذف خطوة ولا يُقصَّر إثبات ليمرّ الوقت.
    #
    # [FINAL-RC] ٣٥ ⇐ ٤٥ دقيقة — بالقياس لا بالتقدير: سير رأس CORS (#572) استغرق
    # ٢٧:٥٣، وخطوتا إغلاق التمرين اللتان دخلتا هذا السير بالالتقاء قِيستا على
    # فرعهما (#566): ١:٠٠ + ٢:٥٩. فالمجموع ≈ ٣٢ من ٣٥ — ٩١٪ من الميزانية، وهو
    # بعينه «السير الذي يعيش على سقفه» الموصوف أعلاه. لا خطوة حُذفت ولا إثبات قُصِّر.
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22 (+ npm cache)
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # ---- static checks (fail fast) ----
      - name: Typecheck
        run: npx tsc -b --noEmit

      - name: Lint (--max-warnings 0)
        run: npx eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0

      # ---- production build ----
      # ⚠️ [RED-TEAM-FINAL] **`npm run build` لا `vite build`.**
      # كانت الخطوة `npx tsc -b && npx vite build`، فتتخطّى
      # `scripts/food-production/ensure-search-assets.mjs` الذي يعيش **داخل**
      # سكربت `build` وحده. فلا تُولَّد `public/food/search/` (مولَّدة ولا
      # تُلتزم)، ويسقط `test:search-quality` بـ١٦ من ٦٧ في CI بينما يمرّ ٦٧/٠
      # محلّيًا حيث الأصول حاضرة من بناءٍ سابق. أي أن السير **كان يبني شيئًا
      # غير الذي يُنشر** — والأحمر مقيم منذ 39fd151b (٢٧ أغسطس).
      - name: Production build
        run: npm run build

      # Informational, NON-GATING.
      # ⚠️ [RED-TEAM-FINAL] صُحّح النصّ: كان يقول «entry/boot budgets pass»
      # وأن الفائض في حزمة كسولة (NutritionView). **والمقيس عكسه**: الحزمة
      # الكسولة الأكبر ١١١٫٥KB تحت حدّها (١٣٠KB)، والفائض في المدخل نفسه —
      # entry ١١٦٫٨KB (الحدّ ٨٠) وboot ١٧٩٫٢KB (الحدّ ١٤٠). تعليقٌ يطمئن على
      # ما لم يُقَس أخطرُ من غياب التعليق. يبقى غير حاكم، ويبقى دَينًا مسمّى.
      - name: Perf budget (informational, non-gating)
        continue-on-error: true
        run: node scripts/check-perf-budget.mjs

      - name: Food database quality proof
        run: npm run test:food-db

      - name: Full deterministic test gate
        run: npm run test:gate

      # ── رحلات التكليف على Postgres **حقيقي** ────────────────────────────
      # خارج `test:gate` المحلّي عمدًا وبنفس منطق خطوات المتصفّح (§4.0): الطقم
      # يحتاج عنقودًا حيًّا، وإلزام كل مطوّر بتشغيله قبل كل بوابة كلفةٌ أعلى من
      # عائدها. **والمقابل الملزم أنه يعمل هنا** — لا استثناء صامت.
      #
      # ولماذا لا يكفي صندوق PGlite الموجود: هو **اتصال واحد**، فلا يُختبَر فيه
      # تسابقٌ ولا قفلُ صفّ. وفحص «كودٌ بحصّة واحدة من متصفّحين معًا» يمرّ فيه
      # مجّانًا — يبدو أخضر وهو غير مُختبَر (§4.2).
      - name: Start PostgreSQL 16 for commissioning journeys
        run: |
          set -euo pipefail
          PGBIN=$(ls -d /usr/lib/postgresql/*/bin | tail -1)
          sudo mkdir -p /var/lib/postgresql/qimmah-ci
          sudo chown -R postgres:postgres /var/lib/postgresql
          sudo chmod 700 /var/lib/postgresql/qimmah-ci
          sudo -u postgres "$PGBIN/initdb" -D /var/lib/postgresql/qimmah-ci -U postgres --auth=trust -E UTF8
          sudo -u postgres "$PGBIN/pg_ctl" -D /var/lib/postgresql/qimmah-ci \
            -o "-p 55432 -k /tmp -c listen_addresses=127.0.0.1" -l /tmp/pg-ci.log start
          "$PGBIN/pg_isready" -h 127.0.0.1 -p 55432

      - name: Migrations apply without superuser (Supabase-shaped role)
        run: npm run test:nonsuperuser-apply
        env:
          QIMMAH_PG_PORT: '55432'

      - name: SQL paste bundles (no drift · clean apply · re-paste safe)
        run: npm run test:staging-sql
        env:
          QIMMAH_PG_PORT: '55432'

      - name: Commissioning journeys (real Postgres, all migrations)
        run: npm run test:commissioning

      # ═══ [RED-TEAM-FINAL] الأطقم التي تحتاج عنقودًا — تُشغَّل حيث يوجد ═══
      # ثلاثة أطقم أمنية (ذرّية التجربة · ذرّية الكود · ختم البوّابة سلطةً في
      # القاعدة) موصولةٌ بـ`test:gate` — والبوّابة تسبق إقلاع Postgres في هذا
      # السير، فكانت تُعلن تخطّيها وتخرج بـ0 **فلم تُنفَّذ قطّ**. تُشغَّل هنا
      # بعد العنقود، وغيابُه في هذه الخطوة فشلٌ أحمر لا تخطٍّ.
      - name: Postgres-backed attack suites (trial race · redeem race · gateway stamp authority)
        run: npm run test:attack-pg

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Onboarding v2 browser E2E
        run: npm run test:e2e:onboarding

      # [AUTH-DISCOVERABILITY-001] الرحلة الحقيقية: ضيف على «التقدّم» يجد «تسجيل
      # الدخول»، يدخل ويعود إلى التقدّم نفسه؛ بوّابة Premium تعطي زرّ دخول لا
      # نصًّا؛ الخروج بتأكيد ولا أثر جلسة بعده؛ #/admin كما هو. Supabase مُستبدَل.
      - name: Auth discoverability browser E2E
        run: npm run test:e2e:auth-discoverability

      # [CUSTOM-PLAN-DEADEND-001] رحلة الجدول المخصّص كاملة: أيام → بناء → مراجعة
      # بلا تمارين لها فعل لا زرّ معطَّل → حفظ → ثبات → تعديل بلا تكرار → إلغاء آمن.
      - name: Custom plan browser E2E
        run: npm run test:e2e:custom-plan

      # [STANDALONE-CHROME-001] الشاشات خارج القشرة تحت نتوء محاكًى: الرأس وزرّ
      # الرجوع تحت شريط الحالة، متمرّر خاص للعضوية، والرجوع يعود إلى المصدر.
      - name: Standalone screen chrome browser E2E
        run: npm run test:e2e:standalone-chrome

      # [MOBILE-SHELL-001] القشرة على الجوال كتطبيق: حقول ≥١٦ بكسل (لا تكبير iOS)،
      # لوحة المفاتيح تُقلّص القشرة وتُخفي الشريط والحقل يبقى ظاهرًا، والتراكبات
      # تعيش في body فلا تُحبَس في متمرّر القشرة (محاكاة WebKit). رحلة الأكل كاملة.
      - name: Mobile shell browser E2E
        run: npm run test:e2e:mobile-shell

      # فحص المؤسس اليدوي أسقط ادّعاءً آليًا أخضر: الحقول تقبل ٢٤ و١٧٧ و١٠٦
      # بصريًّا ثمّ تردّ «أكمل الأربعة». والبوّابة كانت خضراء بصدق — تفحص
      # `foldDigits` لا مستهلكها. فهذا الفحص **يقود المتصفّح** على الأرتيفكت.
      - name: Arabic numeral input — live browser
        run: npm run test:e2e:arabic-numerals

      # نداء «اليوم» كان يَعِد بالإحماء ويُسلّم قائمة التمارين — النيّة تُفقد
      # عند الحدّ بين الشاشتين. يُفحص حيًّا لأن العطل كان في التوصيل لا في الوحدة.
      - name: Today warm-up CTA — live browser
        run: npm run test:e2e:warmup-cta

      # مسار الكتابة تحت استحقاق: بوّابة → تفعيل → إحماء → تسجيل مجموعة → إنهاء.
      - name: Workout write-path (entitled) — live browser
        run: npm run test:e2e:workout

      # بقيّة مسار الكتابة: تغذية وماء، وقياسات وتقدّم.
      - name: Nutrition write-path (entitled) — live browser
        run: node scripts/e2e/nutrition-reliability.mjs

      - name: Progress & measurements write-path (entitled) — live browser
        run: node scripts/e2e/progress-reliability.mjs

      # اتّصال الجلسة: الشريط في كل شاشة، والتمرير يعود لأعلى عند كل تمرين،
      # ووعد البطاقة = ما يُسلَّم فعلًا. يحتاج بناء تقليد كبقيّة رحلات المتصفّح.
      - name: Workout session continuity — live browser
        run: npm run test:e2e:workout-continuity

      # [WORKOUT-CLOSURE-001] رحلة المؤسس الحرفية — ٤ أيام × ٧٥ دقيقة: الوعد =
      # الخطة = المقام الحيّ = الجلسة المحفوظة، مع استئناف منتصف الجلسة. هذه
      # التركيبة بعينها كانت تفتح «١ من ١» — ولم يكن لها إثبات متصفّح واحد.
      # تشغيل مباشر: يعيد استخدام أرتيفكت التقليد الذي بنته خطوة الاتّصال
      # أعلاه (ميزانية السير ٣٥ د)، والسكربت نفسه يرفض أرتيفكتًا غير مقلَّد.
      - name: Founder exact workout journey (4d × 75min) — live browser
        run: node scripts/e2e/founder-exact-workout.mjs

      # [WORKOUT-CLOSURE-001] برامج Dataset B: كانت الرحلة تقف عند الخطة
      # المحفوظة ولا تضغط «ابدأ» — ولم تكن موصولة بأي workflow أصلًا. الآن:
      # ٨ برامج × كل جلساتها تُبدأ فعلًا ويُقارن مقام كلٍّ بعدد يومها المحفوظ.
      # تشغيل مباشر على نفس أرتيفكت التقليد — كخطوتي التغذية والتقدّم أعلاه.
      - name: Dataset B programs + live session matrix — live browser
        run: node scripts/e2e/dataset-b-programs.mjs

      # رحلة مراجعة المؤسس على **نفس أرتيفكت المعاينة المنشور** — لا على بناء
      # تقليد. فحص المؤسس الحيّ أسقط ادّعاءً أخضر مصدره أن الرحلات كانت تعمل
      # على بناء آخر لا يفتحه المؤسس أبدًا.
      - name: Founder QA journey (founder_preview artifact) — live browser
        run: npm run test:e2e:founder-qa

      - name: Upload dist artifact
        if: success()
        # [CTO-87] الرفع **دليل بناء لا شرط إطلاق**: لا خطوة تُنزّله
        # (`download-artifact` غير موجود في المستودع)، ولا وظيفة نشر تعتمد عليه —
        # النشر يجري عبر تكامل Cloudflare Pages مع Git لا عبر هذا الأرتيفكت.
        # وحين تمتلئ حصّة تخزين الحساب يفشل الرفع فيُحمِّر البوابة كلّها رغم نجاح
        # كل فحوص الجودة قبله — أحمر بنية تحتية يُقنّع أي أحمر كودي قادم (§4.0).
        # `continue-on-error` يفكّ هذا التقنيع **دون المساس بأي فحص**: الخطوات
        # الثمانية قبله تبقى إلزامية كما هي، ولم يُحذف الرفع بل صار غير حاجب.
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ github.sha }}
          path: dist
          if-no-files-found: error
          retention-days: 3

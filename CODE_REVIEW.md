# مراجعة قِمّة الشاملة

**تاريخ المراجعة:** 28 يوليو 2026  
**المستودع:** `/Users/ziyad/qimmah-deploy`  
**الالتزام المرجعي:** `cb43ef48a406b97c9a7f59006c95b0aeb5b7b647`  
**الفرع عند الإغلاق:** `fix/audit-cleanup-wave`

## 1. الحكم التنفيذي

التطبيق **قابل للبناء ويحتوي منتجًا فعليًا واسع الوظائف**، وليس واجهة تجريبية فقط. النواة المحلية — الإعداد، توليد الخطة، تنفيذ التمرين، السجل، القياسات، التغذية اليومية، والالتزامات — موجودة وتعمل على مستوى الكود. لكنه **غير جاهز للإطلاق العام بثقة بيانات كاملة** قبل إغلاق مسار سلامة البيانات، وتحديد حقيقة المزامنة السحابية، وإضافة حد أدنى من الاختبارات.

الحكم المختصر:

- **جودة البناء:** جيدة؛ TypeScript وESLint والبناء الإنتاجي نجحت.
- **اكتمال المنتج المحلي:** جيد، مع فجوات في الاستمرارية وحالات الفشل.
- **سلامة البيانات:** تحتاج إصلاحًا قبل إضافة ميزات كبيرة.
- **المزامنة السحابية:** موجودة جزئيًا في الكود لكنها غير موصولة بتدفق المنتج، ولا تغطي كامل بيانات المستخدم.
- **ميزة الخطوات والصحة:** العد اليدوي مكتمل جزئيًا؛ Apple Health/Health Connect غير مفعّلين.
- **العربية وRTL:** الأساس جيد، لكن توجد عيوب اتجاه واستجابة ووصولية.
- **الاختبارات:** لا توجد حزمة اختبارات آلية تحمي المسارات الحرجة.
- **جاهزية الإطلاق:** **مشروطة/غير مجازة حاليًا**.

## 2. نطاق المراجعة وحالة المستودع

راجعت كود التطبيق، بنية البيانات، التخزين المحلي، المزامنة، المصادقة، واجهات المستخدم، ميزة الخطوات، الوثائق، وإعدادات البناء.

هناك حالتان يجب عدم خلطهما:

1. الالتزام `cb43ef4` هو اللقطة المرجعية التي بدأت عليها المراجعة، ويطابق فرع النسخة الاحتياطية البعيد `backup/claude-p25-3d-20260725`.
2. أثناء المراجعة تغيّر الفرع إلى `fix/audit-cleanup-wave` وظهرت تعديلات محلية غير ملتزمة، تشمل تعديلات في التخزين والمزامنة والخطوات وحذف ملفات قديمة. لم ألمس هذه التعديلات ولم أعد ضبطها.

لذلك تصنّف النتائج إلى:

- نتائج مثبتة على الالتزام المرجعي وما زالت ظاهرة في الحالة الحالية.
- ملاحظات تخص الحالة المحلية الحالية، ويجب إعادة تثبيتها بعد تثبيت/التزام موجة التنظيف.

## 3. خريطة المنتج

| المجال | الحالة | الدليل والحكم |
|---|---|---|
| الإعداد الأولي وتوليد الخطة | مكتمل فعليًا | ملف التعريف الأولي والحاسبات ومولد الخطة تنتج خطة قابلة للاستخدام وتُحفظ محليًا. |
| الدخول كضيف | مكتمل فعليًا | التطبيق يعمل محليًا دون حساب. |
| الحساب والمصادقة | جزئي | Supabase Auth موجود، لكن تجربة فشل الشبكة غير مكتملة، والمزامنة لا تبدأ تلقائيًا بعد الدخول. |
| تمرين اليوم | مكتمل مع عيب سلامة | تنفيذ الجلسة وتسجيل المجموعات والإنهاء موجود؛ فشل الحفظ قد يظهر للمستخدم كنجاح. |
| سجل التمارين والأرقام الشخصية | مكتمل محليًا | توجد جلسات وسجل تمارين وقياسات وتقدم. |
| التغذية والماء | جزئي | تسجيل اليوم الحالي موجود؛ تفاصيل الأطعمة لا تنتقل كاملة إلى سجل الأيام بعد rollover. |
| المكملات والأدوية والالتزامات | مكتمل محليًا | التسجيل والتخصيص موجودان، مع تكرار واجهات الاختيار. |
| القياسات والتقدم وعضلات الجسم | مكتمل فعليًا | القياسات، مخططات التقدم، وتغطية العضلات/النموذج ثلاثي الأبعاد موجودة. |
| الخطوات | مكتمل يدويًا جزئيًا | إدخال يومي وهدف ومخطط أسبوعي موجودة؛ لا يوجد تكامل صحي فعلي ومشاكل الحفظ/منتصف الليل قائمة. |
| التصدير والاستيراد وإعادة الضبط | جزئي عالي الحساسية | الوظائف موجودة، لكن الاستيراد يحتاج تحققًا بنيويًا صارمًا ورسائل فشل موحدة. |
| المزامنة السحابية | موجودة تقنيًا وغير متاحة منتجيًا | `fullSync()` موجودة في `src/lib/syncService.ts` بلا مستدعٍ في التطبيق. |
| التذكيرات | شكلية | تفضيلات واجهة موجودة، لكن لا توجد جدولة إشعارات فعلية. |
| الإنجليزية | معطلة | اللغة مضبوطة على العربية، والنصوص تشير إلى أن الإنجليزية قادمة لاحقًا. |
| Apple Health/Health Connect | معطل/قادم | توجد وصلة إدخال خارجية، لا تكامل منصة أو صلاحيات أو مزامنة فعلية. |
| البيانات التجريبية | معزولة غالبًا | `#/demo` يحتوي رياضيًا وخططًا وبيانات غنية نموذجية؛ لا يُعد دليلًا على بيانات مستخدم حقيقية. |
| الوسائط التعليمية | جزئية | توجد عناصر تمارين بلا وسائط مكتملة؛ نحو 67 عنصرًا يستخدم بدائل/placeholder من أصل نحو 170. |
| الصفحات القانونية والتواصل و404 | موجودة | المسارات موجودة، لكن زر الرجوع في بعض الصفحات غير آمن عند فتح رابط مباشر. |

## 4. تدفق البيانات الفعلي

المسار المحلي الأساسي:

`واجهة المستخدم → دوال المجال → safeStorage/localStorage → إعادة القراءة إلى الحالة`

المسار السحابي المقصود:

`تسجيل الدخول → syncService → Supabase Auth/جداول المستخدم → دمج إلى localStorage`

المشكلة أن المسار الثاني غير موصول فعليًا:

- `fullSync()` وعمليات الدفع/السحب معرفة في `src/lib/syncService.ts` ولا يظهر لها استدعاء من الواجهات.
- شاشة الإعدادات تعرض التصدير والاستيراد وإعادة الضبط، ولا تعرض إجراء مزامنة.
- نجاح تسجيل الدخول يدخل التطبيق دون تنفيذ مزامنة.
- `markPendingSync()` يظهر في مسارات محدودة، وليس بروتوكول طابور شامل لكل كتابة محلية.

حتى لو وُصلت المزامنة، تغطيتها الحالية جزئية. الكود يزامن أساسًا:

- جلسات التمرين
- سجل التمارين
- القياسات
- بعض السجلات اليومية

بينما بيانات محلية أخرى مثل تفاصيل التغذية والماء والمكملات والأدوية والتخصيص والملف والتفضيلات والخطوات والتذكيرات لا تدخل كلها في manifest سحابي واضح. توجد جداول في مخطط Supabase لبعضها، لكن وجود الجدول لا يعني أن مسار المزامنة يستخدمه.

**القرار المطلوب:** إما تعطيل/وصف المزامنة بوضوح كميزة تجريبية، أو تصميم manifest واحد لكل بيانات المستخدم وربط دورة مزامنة يمكن مراقبتها واختبارها.

## 5. مراجعة ميزة `steps-health`

### ما يعمل

- تخزين عدد الخطوات لكل تاريخ.
- هدف يومي قابل للتعديل.
- إعادة الضبط.
- عرض أسبوعي.
- عزل بيانات العرض التجريبي عن واجهة التقدم الحقيقية.
- لا توجد مطالبة مضللة بأن Apple Health يعمل الآن؛ النص يصفه كميزة قادمة.

### ما لا يكتمل

1. **فشل الكتابة غير ظاهر للمستخدم.**  
   `src/lib/stepCounter.ts` ينفذ كتابات متعددة، و`src/components/StepCounterCard.tsx` يحدّث الواجهة بعد الاستدعاء بلا نتيجة نجاح موحدة. يمكن أن يظهر رقم جديد رغم عدم ثباته.

2. **الكتابة ليست عملية واحدة.**  
   قيمة الخطوات ومصدرها موزعان على مفاتيح/كتابات، ما يسمح بحالة نصف محفوظة عند امتلاء التخزين.

3. **الانتقال عند منتصف الليل ناقص.**  
   البطاقة تتفاعل مع focus وvisibility وبعض التفاعل، لكن لا يوجد مؤقت يصفر/يعيد تحميل اليوم إذا بقي التطبيق مفتوحًا في الواجهة الأمامية عبر منتصف الليل.

4. **مصدر البيانات لا ينعكس في الواجهة.**  
   البنية تقبل مصدرًا خارجيًا، بينما البطاقة تعرض القيمة كإدخال يدوي دائمًا.

5. **وصلة الصحة غير جاهزة أمنيًا أو منتجيًا.**  
   `window.QimmahSteps` تُعرّف في `src/main.tsx` لكل تشغيل ويب، ومدخل المصدر الخارجي لا يملك عقدًا/versioning أو تحققًا قويًا كافيًا. لا توجد صلاحيات منصة، توقيع/مصادقة رسالة، أو حل تعارض.

6. **المخطط الأسبوعي غير قابل للقراءة المساعدة.**  
   الأعمدة عبارة عن `div` وقيمها تعتمد بصريًا على الارتفاع/العنوان، بلا جدول أو وصف وصولي مكافئ.

**الحكم:** الميزة مناسبة كـ **عداد يدوي محلي تجريبي**، وليست تكامل صحة. لا ينبغي تشغيل الوصلة الخارجية قبل تعريف عقد البيانات والملكية وحل التعارض والاختبارات.

## 6. نتائج سلامة البيانات والمنطق

### P0 — نجاح زائف عند فشل حفظ التمرين

السلسلة المتأثرة تمتد من `safeStorage` إلى `historyStore`/سجل الجلسة ثم `finishWorkout` و`WorkoutView`. طبقة التخزين تسجل الخطأ، لكن النتيجة لا تصل إلى الشاشة، فتنتقل الواجهة إلى ملخص النجاح.

**إثبات قابل لإعادة الإنتاج:**

- جرى استبدال `localStorage.setItem` بمحاكاة ترمي `QuotaExceededError`.
- استدعاء `persistFinishedSession` عاد طبيعيًا.
- سجل الفشل احتوى `qimmah:history:stamps:v1` ونتيجة `quota`.
- الشاشة الحالية لا تملك نتيجة فشل تمنع ملخص الإكمال.

النتيجة المرصودة:

```json
{
  "saveFailure": {
    "key": "qimmah:history:stamps:v1",
    "result": "quota"
  },
  "persistReturnedNormally": true
}
```

**الأثر:** خسارة تمرين المستخدم مع تأكيد نجاح مضلل.

### P0 — الاستيراد لا يملك مخطط ثقة موحدًا

ملف الاستيراد مدخل غير موثوق. التحقق الحالي لا يضمن البنية والإصدارات والحدود لكل الحقول قبل الدمج/الكتابة. كما أن مسار الاستيراد المتقدم يمكن أن يبتلع الخطأ بصمت، بينما الإعدادات تعرضه؛ السلوكان غير متسقين.

التحقق نفسه يجب أن يطبق على:

- ملفات الاستيراد.
- بيانات Supabase المسحوبة.
- رسائل/قيم وصلة الخطوات الخارجية.
- بيانات `localStorage` القديمة أو المعدلة يدويًا.

### P0 — لا توجد Error Boundary على مستوى التطبيق

خطأ render أو بيانات تالفة في شاشة عميقة يمكن أن يسقط التطبيق كله بلا مسار تعافٍ. يلزم حاجز أخطاء يحافظ على إمكانية تصدير البيانات أو إعادة الضبط الانتقائي.

### P1 — تفاصيل الطعام تضيع بعد انتقال اليوم

تفاصيل الأصناف محفوظة أثناء اليوم في `src/lib/nutritionTracking.ts`، لكن سجل التاريخ يحتفظ أساسًا بحالة الوجبات والماء ولا ينقل كل تفاصيل الأطعمة. عند بدء يوم جديد تُمسح الحالة اليومية، فتفقد تفاصيل مطلوبة للتاريخ والتحليل.

### P1 — تمرين «الغد» قد يكون خاطئًا

`WorkoutView` يشتق بطاقة الغد بتحريك فهرس الخطة، لا بالبحث عن تاريخ التدريب المجدول التالي.

**إثبات بتاريخ فعلي:**

- التاريخ: الثلاثاء 28 يوليو 2026.
- الجدول: السبت/الثلاثاء/الخميس.
- خطة الأيام: A/B/C.
- خطة اليوم الصحيحة: B.
- أقرب تدريب بعده: الخميس، وخطته C.
- نتيجة معادلة الواجهة الحالية: A.

النتيجة المرصودة:

```json
{
  "today": "b",
  "nextScheduledTraining": "c",
  "workoutViewNextLabel": "a"
}
```

يجب دمج الإصلاح مع حالة «يوم الراحة» بدل ترقيع النص منفصلًا.

### P1 — رفض Promise في تسجيل الدخول يجمّد حالة التحميل

`LoginView` يضبط `busy=true` ثم ينتظر المصادقة، ويعيده بعد نجاح المسار فقط. استدعاءات Supabase في سياق المصادقة غير محاطة دائمًا بـ`try/finally`. رفض اتصال فعلي قد يبقي الزر مشغولًا بلا رسالة وتعافٍ.

## 7. المزامنة وحدود الثقة والخصوصية

أهم المخاطر:

- بيانات السحابة يجب معاملتها كمدخل غير موثوق، لا ككائنات TypeScript موثوقة.
- الدمج الحالي يحتاج schema/version ومهاجرة وحدود حجم وقيم وتواريخ.
- لا توجد سياسة تعارض واضحة: آخر كتابة، دمج حقل بحقل، أم مصدر صاحب سلطة.
- تغطية المزامنة جزئية، ما قد يوحي بنسخة احتياطية كاملة وهي ليست كذلك.
- وثائق المنتج وREADME تقول في مواضع إن البيانات لا تغادر الجهاز أو لا يوجد backend، بينما Supabase والمصادقة والمزامنة موجودة في الكود.
- README يوحي أحيانًا بعدم الحاجة إلى متغيرات بيئة، في حين أن `.env.example` يشرح إعداد Supabase.

قبل الإطلاق يجب توحيد ثلاثة أشياء: **سلوك الكود، نصوص الواجهة، وسياسة الخصوصية/README**. لا يكفي تعديل الوثائق وحدها إن بقيت ميزة غامضة.

## 8. العربية وRTL والاستجابة

### عيوب مثبتة

- `StartView` يجمع موضعًا منطقيًا RTL مع `translateX` فيزيائيًا؛ عنصر المنتصف قد ينزاح في RTL.
- أسهم التقدم في `DashboardView` و`ProfileView` تُدار في RTL بطريقة غير متسقة؛ بعضها يشير عكس اتجاه الانتقال المتوقع.
- معادلة التغذية في `NutritionView` تستخدم صفًا غير قابل للالتفاف مع خلايا وعمليات متعددة، فتتعرض للضغط عند 320px.
- تسميات صغيرة جدًا (10–12px) مع قص/تباين ضعيف تزيد المشكلة على الشاشات الضيقة.
- الإنجليزية معطلة، لكن النصوص العربية موزعة مباشرة في عشرات الملفات بدل مرور كامل عبر قاموس مركزي؛ إعادة تفعيل الإنجليزية الآن ستنتج واجهة مختلطة.

### مصفوفة حالات الشاشة

| الشاشة/المجال | فارغ | تحميل | فشل | الملاحظة |
|---|---:|---:|---:|---|
| البداية/الإعداد | جزئي | غير بارز | جزئي | يلزم منع فقد تقدم الإعداد ورسالة فشل الحفظ. |
| تسجيل الدخول | موجود | موجود | ناقص | رفض الشبكة قد يجمّد `busy`. |
| الرئيسية/اليوم | موجود جزئيًا | لا يلزم غالبًا محليًا | ناقص | أخطاء parsing/render تحتاج Error Boundary. |
| تمرين اليوم | موجود | محلي | خطير | فشل الحفظ يظهر كنجاح. |
| التاريخ والتقدم | موجود | غير موحد | ناقص | البيانات التالفة/القديمة تحتاج recovery. |
| التغذية | موجود جزئيًا | محلي | ناقص | انتقال اليوم يفقد تفاصيل. |
| الخطوات | موجود | محلي | ناقص | فشل الحفظ ومنتصف الليل والمصدر غير معروضة بصدق. |
| الإعدادات/الاستيراد | موجود | موجود جزئيًا | غير موحد | مسارا الاستيراد يعالجان الخطأ بشكل مختلف. |
| التخصيص | موجود | محلي | ناقص | pickers متكررة والاستيراد المتقدم صامت. |
| القانونية/التواصل/404 | موجود | لا ينطبق | جزئي | `history.back()` قد يخرج الموقع عند الدخول المباشر. |
| المزامنة | غير متاحة | غير متاحة | غير متاحة | لا توجد واجهة حالة أو تقدم أو تعارض لأن المسار غير موصول. |

## 9. الوصولية

### P1 — تباين الألوان

- البرتقالي الأساسي `#f26a21` مع نص أبيض يقارب `3.06:1`، ويفشل WCAG AA للنص العادي.
- `#69707c` على السطح الداكن `#181b21` يقارب `3.46:1`، ويستخدم في نصوص صغيرة؛ يفشل AA.

### P1 — الحوارات والـpickers

خمسة pickers متشابهة في الالتزامات والمكملات والأدوية والمكونات والتمارين تفتقد بعض أو كل:

- `role="dialog"`
- `aria-modal="true"`
- اسمًا وصوليًا
- الإغلاق بمفتاح Escape
- حبس التركيز وإعادته إلى المشغّل

بعض حوارات التمرين تملك `role` لكنها بلا اسم وصولي أو إدارة تركيز كاملة.

### P1 — تسميات وعناصر تفاعلية

توجد أزرار أيقونية بلا `aria-label` واضح، ومدخلات لا ترتبط كلها بـ`label`. المخطط الأسبوعي للخطوات يحتاج تمثيلًا نصيًا/جدوليًا مكافئًا.

**منع الرجوع:** أضف `eslint-plugin-jsx-a11y` إلى lint، واختبارات axe للواجهات المشتركة، لا إصلاحات يدوية بلا بوابة.

## 10. الأداء والحزمة

البناء الإنتاجي بتاريخ المراجعة:

```text
dist/assets/index-B8kgSEXr.js   1,025.82 kB
gzip                             276.82 kB
```

Vite أصدر تحذيرًا لأن chunk يتجاوز 500 kB. السبب البنيوي: استيرادات routes ثابتة، مع النموذج ثلاثي الأبعاد وبيانات الكتالوج ومحررات كبيرة في حزمة البداية.

التوصية:

- تحويل الشاشات الثقيلة إلى `React.lazy`/dynamic imports.
- فصل نموذج 3D والمحررات والبيانات الضخمة.
- قياس chunk graph بعد كل فصل.
- وضع budget للحزمة في CI بدل الاكتفاء بتحذير.

## 11. جودة الكود والدَّين التقني

### تكرار

- خمسة تطبيقات متشابهة لـpicker overlay ينبغي توحيدها في مكوّن حوار/اختيار واحد.
- منطق الاستيراد ورسائل الفشل موزع بين الإعدادات والتخصيص.
- منطق الكتابة المحلية يحتاج نتيجة موحدة بدل أن يقرر كل مستهلك كيف يتعامل مع الفشل.

### كود غير مستخدم أو متوقف

- `src/config/theme.ts` بلا مستورد ظاهر.
- `useHashRoute` غير مستخدم.
- `getLanguage` و`setLanguage` غير مستخدمتين حاليًا.
- إجراءات وجبات محكومة بثابت `false`.
- موجة التنظيف الحالية تحذف ملفات landing/customizer/data قديمة بلا مستوردين ظاهرين؛ نجاح lint وحده لا يثبت أمان الحذف، لذلك يجب تثبيتها ببناء واختبارات مسارات.

### ملفات كبيرة تستحق التفكيك بعد حماية السلوك بالاختبارات

| الملف | الحجم التقريبي | النوع/المخاطرة |
|---|---:|---|
| `src/lib/planGenerator.ts` | 921 | منطق مجال مرتفع المخاطرة |
| `src/lib/historyStore.ts` | 822 | تخزين ومهاجرات مرتفعة المخاطرة |
| `src/components/PlanBuilder.tsx` | 794 | حالة وواجهة متضخمة |
| `src/components/WorkoutMode.tsx` | 755 | آلة حالة جلسة |
| `src/components/BodyModel3D.tsx` | 510 | رسم وتفاعل |
| `src/lib/onboardingProfile.ts` | 445 | تخزين/مهاجرات |
| `src/views/TodayView.tsx` | 441 | شاشة متعددة المسؤوليات |
| `src/lib/syncService.ts` | 381 | تزامن ودمج حساس |
| `src/views/DashboardView.tsx` | 359 | شاشة متعددة الكتل |
| `src/lib/calculators.ts` | 344 | منطق مجال |
| `src/components/QuickMealLogger.tsx` | 335 | حالة وتغذية |
| `src/views/CustomizationCenter.tsx` | 318 | محرر متعدد المسؤوليات |
| `src/views/ExerciseDetailView.tsx` | 311 | شاشة تفاصيل |

ملفات البيانات الكبيرة (`foodItems.ts`، `strings.ts`، بيانات المجسم والتمارين) ليست مشكلة لمجرد عدد الأسطر، لكن ينبغي فصلها عن حزمة البداية والتحقق من صحتها آليًا.

## 12. الأمن

لم يظهر سر صريح ملتزم في الفحص السابق، واستخدام مفاتيح Supabase العامة ليس سرًا بحد ذاته إذا كانت RLS صحيحة. لكن لا يمكن منح اعتماد أمني كامل دون:

- فحص سياسات RLS على مشروع Supabase فعلي.
- اختبار أن المستخدم A لا يقرأ/يكتب بيانات المستخدم B.
- تقييد أحجام ومدى ملفات الاستيراد والصفوف السحابية.
- منع prototype pollution/حقول غير متوقعة عبر schema صارم.
- تعريف عقد آمن لوصلة الخطوات أو إزالتها من الويب حتى وجود المستهلك الأصلي.
- إكمال `npm audit`؛ لم يُنفذ لأن الوصول إلى سجل npm وإرسال metadata للحزم لم يكن مصرحًا به في بيئة المراجعة.

## 13. الاختبارات والتشغيل وجاهزية الإطلاق

لا يوجد أمر `test` حاليًا. هذا أخطر من مجرد نقص تغطية، لأن أكثر المنطق حساسية موجود في تواريخ ومهاجرات ودمج وتخزين.

فحوص الإغلاق على الحالة المحلية الحالية نجحت:

```text
npm run typecheck  ✅
npm run lint       ✅
npm run build      ✅
```

لكن البناء يمر بلا اختبارات سلوك، وESLint الحالي لا يلتقط مخالفات JSX accessibility.

**بوابة الإطلاق المقترحة:**

- صفر فشل TypeScript/ESLint.
- اختبارات وحدة لمسارات التخزين والتواريخ والمزامنة.
- اختبارات تكامل للاستيراد/التصدير وفشل quota.
- اختبار RLS بحسابين.
- اختبار UI على 320/768/1280 وبالعربية RTL.
- axe بلا مخالفات حرجة/عالية.
- budget لحزمة البداية.
- تطابق نصوص الخصوصية مع السلوك الفعلي.

## 14. خطة التنفيذ المرتبة

### نقطة الصفر — تثبيت خط الرجوع

قبل أي إصلاح جديد:

- لا تُحذف التعديلات المحلية الحالية ولا يعاد ضبطها.
- ثبّت موجة `fix/audit-cleanup-wave` في commit مستقل بعد مراجعة diff.
- المرجع البعيد الآمن الموجود: `origin/backup/claude-p25-3d-20260725` عند `cb43ef4`.
- لكل مهمة أدناه commit مستقل؛ الرجوع يكون بعكس commit المهمة، لا بإعادة ضبط المستودع كله.

### T0 — تأسيس الاختبارات

**الاعتماديات:** لا شيء.  
**الملفات المتوقعة:** `package.json`، إعداد Vitest/jsdom، مجلد `src/**/*.test.ts(x)`.  
**الاختبارات الأولى:** safeStorage quota/unavailable، تاريخ خطة الغد، parsing بيانات قديمة، rollover يوم التغذية والخطوات.  
**التحقق:** `npm test` ثم typecheck/lint/build.  
**الرجوع:** commit مستقل لإعداد الاختبارات.

### T1 — جعل إنهاء التمرين صادقًا وذرّيًا قدر الإمكان

**الأولوية:** P0. **يعتمد على:** T0.  
**الملفات:** `safeStorage.ts`، `historyStore.ts`، ملفات جلسات/سجل التمرين، `finishWorkout.ts`، `WorkoutView.tsx`.  
**التنفيذ:** نتيجة موحدة discriminated union لكل كتابة، تجميع خطة الحفظ، وعدم عرض النجاح إلا بعد ثبات الحد الأدنى الضروري. وفر retry/export عند الفشل.  
**الاختبارات:** quota، storage unavailable، فشل كتابة وسط السلسلة، نجاح كامل، عدم تكرار الجلسة بعد retry.  
**التحقق اليدوي:** إكمال تمرين مع تخزين يعمل ثم مع quota مصطنع.  
**الرجوع:** commit `T1` وحده؛ لا تغيّر schema بلا مهاجرة عكسية.

### T2 — مخطط تحقق موحد لكل الحدود غير الموثوقة

**الأولوية:** P0. **يعتمد على:** T0.  
**الملفات:** وحدة schema/version جديدة، `SettingsView.tsx`، `CustomizationCenter.tsx`، `historyStore.ts`، `syncService.ts`، `stepCounter.ts`.  
**التنفيذ:** schema بإصدار، حدود أحجام وقيم، normalizers ومهاجرات، ورسالة خطأ واحدة بلا كتابة جزئية. يمكن استخدام Zod بعد قرار dependency أو مخطط يدوي typed إذا أريد إبقاء الحزمة خفيفة.  
**الاختبارات:** JSON تالف، حقول ناقصة/زائدة، NaN وتواريخ مستحيلة، payload ضخم، صف Supabase قديم، رسالة خطوات غير صالحة.  
**التحقق:** round-trip export/import ومقارنة snapshot، واختبار سحب سحابي مع fixtures.  
**الرجوع:** الإبقاء على قارئ الإصدار السابق دورة إصدار واحدة.

### T3 — Error Boundary ومسار تعافٍ

**الأولوية:** P0. **يعتمد على:** T1 وT2 لرسائل أدق.  
**الملفات:** `App.tsx`، مكوّن ErrorBoundary، شاشة تعافٍ، أدوات export/reset.  
**الاختبارات:** throw أثناء render، بيانات تالفة، نجاح تصدير من شاشة الخطأ.  
**التحقق:** حقن مكوّن يرمي والتحقق من بقاء خيارات الاستعادة.  
**الرجوع:** مكوّن مغلف في commit مستقل.

### T4 — إكمال عداد الخطوات اليدوي وتجميد ادعاء الصحة

**الأولوية:** P1. **يعتمد على:** T0 وT2.  
**الملفات:** `stepCounter.ts`، `StepCounterCard.tsx`، `main.tsx`، `ProgressView.tsx`، `strings.ts`، `resetQimmah.ts`.  
**التنفيذ:** نتيجة كتابة واحدة، rollover timer، عرض المصدر، جدول وصولي، وتعطيل الوصلة العامة أو versioning صارم لها. لا تسمِّها مزامنة صحية قبل تكامل أصلي فعلي.  
**الاختبارات:** fake timers عبر منتصف الليل، quota، تغيير الهدف، reset، مصدر يدوي/خارجي، حل التعارض.  
**التحقق:** يومان افتراضيان، إعادة فتح، focus/visibility، قارئ شاشة.  
**الرجوع:** إبقاء schema القديمة مقروءة.

### T5 — إصلاح «الغد» وبناء حالة يوم الراحة

**الأولوية:** P1. **يعتمد على:** T0.  
**الملفات:** helper جدول التمرين، `WorkoutView.tsx`/ملخص الجلسة، واجهة يوم الراحة.  
**التنفيذ:** البحث عن أقرب تاريخ تدريب فعلي ثم استخراج plan day لذلك التاريخ.  
**الاختبارات:** 28 يوليو 2026، نهاية الأسبوع، يوم راحة، جداول 2/3/4 أيام، عبور شهر/سنة.  
**التحقق:** مقارنة 20 تاريخًا مولدًا مع الجدول.  
**الرجوع:** helper وواجهة في commit واحد قابل للعكس.

### T6 — قرار المزامنة وتنفيذه بصدق

**الأولوية:** P1 قبل الإطلاق. **يعتمد على:** T2.  
**الملفات:** `syncService.ts`، auth context، الإعدادات، manifest بيانات، مخطط Supabase/RLS، نصوص الحالة.  
**الخيار أ:** تعطيل المسار ووصف الحساب بلا وعد نسخ احتياطي.  
**الخيار ب:** ربط `fullSync`، إكمال dataset manifest، شاشة تقدم/فشل/تعارض، وطابور pending شامل.  
**الاختبارات:** حسابان وRLS، offline→online، تعارض جهازين، حذف، صف تالف، انقطاع منتصف المزامنة، idempotency.  
**التحقق:** مشروع Supabase معزول وسجل sync قابل للتدقيق.  
**نقطة توقف:** لا تنشر الخيار ب دون مشروع اختبار وRLS مثبتة.  
**الرجوع:** feature flag للمزامنة ونسخة محلية قبل أول pull.

### T7 — توحيد حقيقة المنتج والخصوصية

**الأولوية:** P1 قبل النشر. **يعتمد على:** قرار T6.  
**الملفات:** README، PRODUCT/PRD/ROADMAP، سياسة الخصوصية، نصوص الحساب والصحة.  
**التحقق:** جدول ادعاء→دليل لكل عبارة عن السحابة والخصوصية واللغات والصحة.  
**الرجوع:** توثيق فقط، commit مستقل.

### T8 — الوصولية كنظام

**الأولوية:** P1/P2. **يعتمد على:** T0.  
**الملفات:** CSS/tokens، مكوّن Dialog/Picker مشترك، الأزرار والمدخلات، ESLint config.  
**التنفيذ:** ألوان AA، dialog semantics، focus trap/return، Escape، labels، جدول خطوات، وتفعيل `jsx-a11y`.  
**الاختبارات:** axe وkeyboard-only وfocus order.  
**التحقق:** نسب تباين فعلية + قارئ شاشة.  
**الرجوع:** tokens ومكوّن الحوار في commitين منفصلين.

### T9 — RTL والاستجابة

**الأولوية:** P2. **يعتمد على:** T0.  
**الملفات:** `StartView.tsx`، `DashboardView.tsx`، `ProfileView.tsx`، `NutritionView.tsx`، CSS.  
**الاختبارات:** screenshots/visual checks عند 320 و375 و768 و1280، RTL، نص مكبّر 200%.  
**التحقق:** لا overflow أفقي، واتجاه الأسهم مطابق للتنقل.  
**الرجوع:** commit لكل مجموعة شاشة.

### T10 — خفض حزمة البداية

**الأولوية:** P2. **يعتمد على:** استقرار السلوك.  
**الملفات:** router/App imports، شاشات 3D، المحررات، إعداد Rollup.  
**الاختبارات:** فتح كل route lazy وفشل تحميل chunk.  
**التحقق:** مقارنة bundle report ووقت التحميل؛ وضع budget متفق عليه.  
**الرجوع:** كل route split في commit مستقل.

### T11 — تفكيك الملفات عالية المخاطرة

**الأولوية:** P2/P3. **يعتمد على:** اختبارات characterization للمجال المعني.  
**الملفات:** `historyStore`، `planGenerator`، `WorkoutMode`، `PlanBuilder`، `syncService`.  
**التنفيذ:** فصل schema/persistence/domain/UI لا مجرد تقطيع أسطر.  
**التحقق:** نفس snapshots ونتائج الخطة قبل/بعد، وفحوص البناء.  
**الرجوع:** ملف واحد/مجال واحد في كل PR.

### T12 — إغلاق التنظيف والكود الميت

**الأولوية:** P3. **يعتمد على:** تثبيت موجة الحذف الحالية.  
**الملفات:** `theme.ts`، hooks/لغة غير مستخدمة، إجراءات الوجبات، الملفات المحذوفة حاليًا.  
**التحقق:** `rg` للمراجع، typecheck/lint/build، smoke لكل route، ومقارنة حزمة.  
**الرجوع:** commit حذف مستقل؛ لا تخلطه بإصلاحات وظيفية.

## 15. ترتيب التنفيذ والاعتماديات

```text
تثبيت الحالة الحالية
        ↓
       T0
    ↙   ↓   ↘
  T1   T2   T5
   ↓  ↙  ↘   ↓
  T3 T4  T6  حالة الراحة
          ↓
         T7

T8 وT9 بعد T0
T10 بعد استقرار المسارات
T11 بعد اختبارات المجال
T12 بعد تثبيت موجة التنظيف
```

التسلسل الموصى به فعليًا: **T0 → T1 → T2 → T3** أولًا، مع إمكان تنفيذ شريحة الهوية البصرية v2.1 بالتوازي لأنها لا تمس البيانات. بعدها T4 وT5، ثم قرار T6 وT7 قبل أي إطلاق أو وعد بقِمّة+.

## 16. ما تم استبعاده وحدود التقرير

- لم أعتبر ملفات `AGENTS.md`/تعليمات الوكيل ثغرة تطبيق؛ هي تعليمات تطوير وليست جزءًا من runtime المنتج.
- لم أعتبر كبر ملفات البيانات وحده عيبًا؛ العيب هو أثرها على الحزمة أو صعوبة التحقق منها.
- لم أنفذ `npm audit` بسبب قيد الوصول الخارجي المذكور.
- لم أتحقق من RLS أو بيانات مشروع Supabase حي؛ الكود المحلي وحده لا يثبتها.
- لم أعدّل أي ملف مصدر أو أحذف أي تغيير قائم؛ الناتج الوحيد لهذه المهمة هو هذا التقرير.

## القرار النهائي

ابدأ فورًا بمسار سلامة البيانات T0–T3. لا تنتظر بقية التحسينات لإصلاح النجاح الزائف والاستيراد وحدود الخطأ. ويمكن تنفيذ الشريحة البصرية v2.1 بالتوازي بشرط ألا تخلط commits أو تغيّر schemas. بعد ذلك أصلح الخطوات وحالة يوم الراحة، ثم احسم حقيقة المزامنة والخصوصية قبل الإطلاق.

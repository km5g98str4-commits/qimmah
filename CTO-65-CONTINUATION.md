# CTO-65-CONTINUATION — تسليم البنود المتبقّية

> **مَن يكمل يبدأ من هنا، لا من تحليل جديد.** كل مرساة أدناه **مقيسة في هذه الجلسة** على
> `main` @ `0133e6f`، لا منقولة من حزمة التسليم بلا تحقّق.
> **الحالة:** ٣ من ٨ منجزة ومدفوعة · ٥ لم تبدأ.

---

## 1. المنجَز — ٣ بنود

| البند | الإيداع | الملفات |
|---|---|---|
| **١ — واجهة حذف الحساب** (حاجز App Store 5.1.1(v)) | `8419b92` | `components/DeleteAccountDialog.tsx` (جديد) · `views/SettingsView.tsx` · `lib/profileV2Model.ts` · `scripts/run-delete-account-proof.mjs` (جديد) · `package.json` |
| **٨ — زرّ المكمّلات يفتح خطوة الروتين** | `c82bc85` | `lib/setupFocus.ts` (جديد) · `sections/CustomizationCenter.tsx` · `views/ProfileV2.tsx` |
| **٧ — رسالة سبب رفض البريد** | `0133e6f` | `views/LoginView.tsx` · `config/strings.ts` |

**ملاحظات تنفيذية تلزم من يكمل:**

- إثبات البند ١ (`test:delete-account`) **مربوط في `test:gate`** ويحمل ٢١ فحصًا. **ساكن عمدًا** لأن عقد `e2e-auth` السلوكي محجوب بـPostgres/Docker وخارج البوابة — وهو سبب بقاء الفجوة بلا كاشف.
- §4.2 مستوفاة: **هُوجم الملف الحقيقي** بحذف فرع `ok === false`، فسقط الإثبات بفحص مسمّى `فرع صريح يعالج ok === false داخل نفس التدفّق`.
- البند ٨ يستخدم `sessionStorage` بمفتاح `qimmah:setup-focus` — **نفس نمط `qimmah:quick-log-intent` القائم**، لا آلية توجيه جديدة.

---

## 2. البنود المتبقّية — مراسٍ محدَّثة ومقيسة

### البند ٢ — مصدر حقيقة واحد للحدود والأهداف 🚫 لم يبدأ · **الأكبر**

**الافتراق مقيس الآن (أرقام مؤكَّدة من الملفّين):**

| المحور | `lib/onboardingV2Flow.ts` | `lib/validation.ts` |
|---|---|---|
| العمر | `AGE_RANGE = { min: 13, max: 100 }` (`:42`) | — |
| الطول | `HEIGHT_RANGE = { min: 120, max: 220 }` (`:43`) | `heightCm: { min: 100, max: 230 }` (`:7`) |
| الوزن | `WEIGHT_RANGE = { min: 30, max: 250 }` (`:44`) | `weightKg: { min: 15, max: 250 }` (`:8`) |

**الأهداف:** `lib/calculators.ts:142-148` يصدّر **خمسة** (`cutting` · `bulking` · `maintenance` · `returning` · `health`) مقابل ثلاثة في مسار الإعداد.

⚠️ **فخّ مؤكَّد:** `returning` و`health` **ليسا زينة**. `calculators.ts:165` يقرأ منهما التسمية (`goalTypeOptions.find(...)`)، والحزمة رصدت مستهلكًا في `planRationale.ts:337`. **إسقاط أيّهما يكسر الخطة صامتًا.**

⚠️ **فخّ ثانٍ اكتشفته أثناء البند ن٢:** رسائل الأخطاء في `validation.ts:21-22` **تحمل الأرقام نصًّا صلبًا** («بين 100 و230 سم» · «بين 15 و250 كجم»). توحيد الحدود بلا توحيد هذه السلاسل يعطي **رسالة تكذب على المستخدم**: يُرفض عند ٣٠ وتقول له ١٥ مسموح.

**نقطة بدء:** ملف واحد يصدّر الحدود والأهداف، يستهلكه المساران، **وتُبنى رسائل الأخطاء منه** لا بنصّ صلب.

**العقود المهدَّدة:** `test:age-13` · `test:minors` · `test:e-guardrail` · `test:body-fields` · `test:formula`.

> ⚠️ **تحذير خاصّ بهذه الجلسة:** البند ن٢ (منجَز) أضاف حالة `'ageBelowMin'` في `onboardingV2Flow.ts:186` تعتمد `AGE_RANGE.min`. **أي توحيد للحدود يجب أن يبقيها عاملة** — لا تُعِد كتابة `validateStep` بلا إبقاء فصل «تحت الحدّ» عن «حقل ناقص».

---

### البند ٣ — قفزة السعرات ٢٬٢٤٥ → ٢٬٦٤٥ 🚫 لم يبدأ · **يُقاس قبل أن يُصلَح**

**الحزمة تصرّح أن سببها فرضية لا قياس.** لم أؤكّدها ولم أنفِها.

**أقرب مرساة رصدتها:** `components/customizer/steps/StepGeneratePlan.tsx:32-36`

```js
const g = generatePlan(p)
targets: g.targets,
targetsMeta: { manuallyEdited: false, lastCalculatedFromProfileHash: undefined, updatedAt: undefined },
```

⇒ الخطوة **تعيد التوليد وتكتب `targets` جديدة**، وتصفّر `targetsMeta` — بما فيه `manuallyEdited` و`lastCalculatedFromProfileHash`. هذا **متّسق مع الفرضية** (إعادة حساب بدل قراءة المحفوظ) لكنه **لا يثبتها**: يبقى أن يُقاس متى تُستدعى هذه الخطوة عند الفتح، وهل الفارق من `activityLevel` غير المضبوط كما رجّحت الحزمة.

**أول خطوة لمن يكمل:** حساب مكتمل → سجّل رقم اللوحة → افتح «تعديل خطتي» → سجّل الرقم → **قِس أي مدخل تغيّر** بين الحالتين قبل لمس أي سطر.

**صلة بقرار مقفل:** رقم مختلف عند الفتح = **تعديل صامت** — يمنعه القرار المقفل ٣ (تعديل الخطة اقتراح بسبب).

---

### البند ٤ — سبب التغيير عند المراجعة 🚫 لم يبدأ

البنية موجودة (`StepReview.tsx`) والناقص عرض **سبب** كل تغيير.

**الأصل الجاهز:** `lib/planRationale.ts` — **يُبنى عليه** ولا يُكتب تعليل ثانٍ، وإلا صار للتطبيق مصدرا تفسير متناقضان.

⚠️ **ملاحظة نطاق من جرد [CTO-64]:** `views/PlanPreviewView.tsx` و`components/plan/PlanWhyPanel.tsx` — وهما واجهتا التعليل الجاهزتان من حزمة `e/plan-preview-host` — **يتيمتان بلا مسار ولا مستورد** (بند «يهدد الإطلاق» رقم ٤ في `FEATURE_INVENTORY.md`). فقد يكون جزء كبير من البند ٤ **توصيلًا لا بناءً**، تمامًا كالبند ١. **يُفحص هذا أولًا قبل كتابة أي مكوّن جديد.**

**العقود المهدَّدة:** `test:plan-builder` · `test:e-calc-explainer`.

---

### البند ٥ — تسريب مصطلح المتقدّم للمبتدئ 🚫 لم يبدأ · **المرساة مؤكَّدة**

**تحقّقتُ من مرساة الحزمة وهي صحيحة:**

```js
// src/lib/profileV2Model.ts:17
const GOAL_AR: Record<CalorieGoal, string> = { cut: 'تنشيف', maintain: 'محافظة', bulk: 'تضخيم' }
// :145  goalLabel: goal ? GOAL_AR[goal] : null,
// :146  planTitle: goalWord ? (ar ? `برنامج ${goalWord}` : `${goalWord} program`) : …
```

`V2_GOAL_MODEL` **لا يُستخدم في الملف الشخصي** — البحث عنه هناك يعود فارغًا كما حذّرت الحزمة.

⚠️ **قياس إضافي يحسم حجم العمل:** `grep level src/lib/profileV2Model.ts` يعطي **أربع نتائج، كلها لخريطة الالتزام الحرارية** (`CommitmentWeek['level']` — شدّة ٠–٣ في `:36` و`:108`). ⇒ **مستوى التدريب `V2Level` غير متاح في النموذج إطلاقًا.** فالجزء الحقيقي من البند هو **تمرير المستوى المحفوظ** — والمصدر `lib/onboardingProfile.ts:234` (`trainingLevel` عبر `levelFromExperience`).

⚠️ **فخّ السقوط الصامت — مؤكَّد بالكود:**

```js
// src/i18n/dict/onboardingIntent.ts:255
return s.goalWording[level ?? 'intermediate']
```

`goalWordingFor(lang, null)` يعيد صياغة المتوسط ⇒ «تنشيف» تعود، **ويبدو البند منجزًا وهو ليس كذلك**. **الاختبار السلبي (`level = null` لا يمرّ صامتًا) شرط قبول لا تحسين.**

**العقد المهدَّد:** `test:onboarding-intent` — ومدّه خارج الإعداد جزء من البند.

---

### البند ٦ — إزالة لغة القالب التجاري 🚫 لم يبدأ · **المواضع مقيسة**

| النصّ | الموضع المؤكَّد |
|---|---|
| «اسم **الصفحة**، الوصف، والنوع» | `components/customizer/steps/StepBasics.tsx:8` |
| «معاينة **صفحتك**» | `i18n/dict/onboarding.ts:573` |
| «**صاحب الصفحة**» | `i18n/dict/onboarding.ts:575` |
| «يظهر في **صفحتك** وفي ترحيب «اليوم»» | `i18n/dict/onboarding.ts:592` |
| «بنجهّز **صفحتك** لأول مرة» | `i18n/dict/onboarding.ts:595` |
| «Shows on your **page**…» | `i18n/dict/onboarding.ts:1020,1233` — **الإنجليزية مصابة أيضًا** |

**القيد المحفوظ:** النصوص القانونية الرسمية (الخصوصية · الشروط) **فصحى ولا تُمس**.

⚠️ **صلة بجرد [CTO-64] §14:** `.claude/rules/product.md` ما زال يصف المشروع «**قالبًا تجاريًا قابلًا للبيع**» و«Frontend فقط بلا backend/مصادقة» — يناقض `CLAUDE.md` §0 وواقع الكود. **هذا مصدر اللغة لا عرَضها.** يُرفع مع البند ولا يُصحَّح اجتهادًا (§7).

**العقود المهدَّدة:** `test:site-truth` · `test:english-content` · `test:tone-debt`.

---

## 3. حالة البوابة عند التسليم

| البوابة | النتيجة |
|---|---|
| `npm run typecheck` | ✅ |
| `npm run lint` | ✅ (`--max-warnings 0`) |
| `npm run build` | ✅ |
| `npm run test:gate` | ✅ **٥٩ إثباتًا · ٢٨٠٣ فحصًا · صفر فشل** |

الأساس قبل هذه البنود كان **٥٨ إثباتًا · ٢٧٨٢ فحصًا**. الزيادة: `test:delete-account` (٢١ فحصًا).

> ⚠️ **§4.0:** البوابة المحلّية أربع، وسير CI يزيد عليها خطوات بمتصفح (`test:e2e:onboarding` · حارس الرحلات) **لم تُقرأ في هذه الجلسة**. أحمر غير مسمّى يجمّد الدمج مهما اخضرّت المحلّية.

---

## 4. تعليمات البدء المباشر

```bash
cd /Users/ziyad/qimmah-deploy
git fetch origin && git checkout main && git pull
npm ci                      # لا npm install — القفل حتمي ويحذف الزائد
git log --oneline -3        # يجب أن يبدأ بـ 0133e6f
npm run test:gate           # خطّ الأساس: 59 إثباتًا · 2803 فحصًا
```

**الترتيب الموصى به** (من ترتيب موجات الحزمة، بعد إسقاط المنجَز):

| الخطوة | البند | لماذا هنا |
|---|---|---|
| ١ | **٥** | مرساة مؤكَّدة وحجم العمل معروف (تمرير `level`) — مكسب مبكّر |
| ٢ | **٦** | نصوص وقواميس، الحارة نفسها التي يمسّها ٥ |
| ٣ | **٣** (قياس فقط) | يُقاس قبل ٢ لأن نتيجته قد تغيّر تصميم مصدر الحقيقة |
| ٤ | **٢** | الأكبر — بعد أن يُعرف ما يحتاجه ٣ |
| ٥ | **٤** | يعتمد على استقرار ٢ و٣ · وافحص أولًا إن كان توصيلًا لا بناءً |

**قواعد سارية على كل بند:**
1. **إيداع مستقل لكل بند** — لا تجميع.
2. **لا تفكيك `pw.valid && eligible12`** في `LoginView.tsx` — العقد يطابقه حرفيًا (`policy-gates-proof.mjs:27`). البند ٧ أُنجز بجانبه لا بداخله؛ احتذِ به.
3. **لا إسقاط `returning`/`health`** — لهما مستهلك فعلي.
4. **الفحص الساكن ليس شهادة وفاة** — قبل حذف أي ملف، `grep` للمراجع الديناميكية موثَّقًا في التقرير.
5. **كل شدّ بوابة يُرفَق بمحاكاة التفاف تفشل بفحص مسمّى** (§4.2) — وتُجرَّب على **الملف الحقيقي** لا على نصّ مُصطنع.
6. **بطاقة البوابة** من `QIMMAH-QUALITY-CHARTER.md` §6 تُملأ في تقرير كل موجة — وأهمّها **سطر «مسار الوصول»**: من أين يبدأ المستخدم وبكم نقرة يصل، **مع فحص الحزمة المبنيّة**. سقوط هذا السطر هو ما أخفى ثلاث شاشات V2 يتيمة عن كل البوابات الخضراء.

---

## 5. فخاخ موحَّدة — مرجع سريع

| الفخّ | الموضع | الأثر إن أُهمل |
|---|---|---|
| تعبير التمكين الحرفي | `LoginView.tsx:52` ↔ `policy-gates-proof.mjs:27` | `test:policy` يسقط بإعادة صياغة لا بتغيير سلوك |
| السقوط الصامت للمستوى | `onboardingIntent.ts:255` (`level ?? 'intermediate'`) | البند ٥ يبدو منجزًا وهو ليس كذلك |
| أرقام الحدود نصًّا صلبًا | `validation.ts:21-22` | رسالة خطأ تكذب على المستخدم بعد توحيد الحدود |
| قيم أهداف لها مستهلك | `calculators.ts:142-148` · `planRationale.ts:337` | كسر صامت في توليد الخطة |
| اسم متاح ملوَّث | زرّ «حذف الحساب» في `SettingsView` | عقد `e2e-auth` يطابق `exact:true` — أي نصّ داخل الزرّ يُسقطه |
| فصل «تحت الحدّ» عن «حقل ناقص» | `onboardingV2Flow.ts:186` | إعادة كتابة `validateStep` تُعيد رسالة الحقول لمن عمره تحت ١٣ |
| ملفات V2 يتيمة | `WorkoutV2` · `ProgressV2` · `NutritionV2` · `PlanPreviewView` | عمل مدموج لا يصل المستخدم — البوابة خضراء وهي تقيس الطبقة الخطأ |

---

## 6. مفتوح للمجلس — لا يُحسم في موجة

### ٦-١ تناقض `recomposition` — هدف ملغى ما زال في الكود بمسارين

`recomposition` هدف **ألغي** وتبقى بياناته عند مستخدمين قدامى، ويُقرأ اليوم بجوابين متناقضين:
`GOAL_TYPE_TO_CALORIE_GOAL` في `config/profileDomain.ts` يعطيه **`maintain`** (نقلًا حرفيًا عن سلوك ما قبل توحيد البند ٢ — لا تغيير سلوك مُهرَّب داخل موجة توحيد)، بينما `GOALTYPE_TO_ONB` في `lib/onboardingProfile.ts` يهاجره إلى **`cut`**. فالمستخدم نفسه يحصل على سعرات صيانة من الحاسبة وعلى مسار عجز من الهجرة.

> **السؤال للمجلس:** هدف ملغى لمستخدمين قدامى — **يُهاجَر إلى هدف حيّ** (وأيّهما: `cut` كما تفعل الهجرة، أم `maintenance` كما تفعل الحاسبة؟) **أم يبقى** بمساره الحراري الحالي بلا هجرة؟ الحسم قرار منتج لا اجتهاد موجة (§7) — والتوحيد على أحد الجوابين **يغيّر سعرات مستخدمين قائمين**، فلا يُنفَّذ بلا قرار.

### ٦-٢ البند ٤ هبط بلا إثبات في البوابة — بند معلّق

`a6cf365` أضاف `lib/planChanges.ts` (١٤٩ سطرًا) و`i18n/dict/planChanges.ts` (٧٥) وكتلة عرض في `StepReview`، **ولم يضف سكربت إثبات ولا عدّل `package.json`** — فمنطق «كان → صار — والسبب» بلا كاشف. §4 يوجب سكربت إثبات بنمط `scripts/run-*-proof.mjs` مربوطًا في `test:gate` لكل إصلاح سلوك حرج، خصوصًا هذا: مخرَجه **سبب سببي معروض للمستخدم**، وهو أخطر ما يُترك بلا حارس.

**المطلوب:** إثبات يغطّي عقد الصدق المُعلَن في رأس `planChanges.ts` — أن ما لم يتغيّر لا يُذكر، وأن التحرير اليدوي يقول «عدّلته بنفسك» ولا يُلفَّق له تعليل هندسي — ومعه محاكاة التفاف تفشل بفحص مسمّى (§4.2). **يملكه كاتب البند ٤.**

---

*مبنيّ على قياس `main` @ `0133e6f` — 2026-08-02. كل رقم ومسار أعلاه مقيس في هذه الجلسة.*
*ذيّل بالقسم ٦ عند إغلاق جلسة البنود ٢·٣·٥·٦ — `main` @ `b04e0a1`.*

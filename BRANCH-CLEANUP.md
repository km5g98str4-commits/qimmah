# جرد الفروع — [CTO-78] الحرف ب

**التاريخ:** 2026-08-04 · **قاعدة القياس:** `main` @ `0d7d84f` ([CTO-76])
**الأداة:** `git rev-list --count main..<ref>` لكل اسم فرع فريد (اتحاد المحلي والبعيد، والأبعد رأسًا هو المقيس).

> **القاعدة الحاكمة المطلقة:** لا يُحذف فرع فيه إيداع غير موجود في `main` إلا بعد وسم `archive/<الاسم>`.
> **حالة التأمين: منجَزة قبل أي حذف** — الـ٧٦ كلها موسومة ومدفوعة إلى `origin`، بتأكيد مضادّ ٧٦/٧٦ مطابق.

---

## الأرقام الحقيقية

| التصنيف | العدد | الإجراء |
|---|---|---|
| **مدموج في `main` بالكامل** (`ahead = 0`) | **245** | حذف مباشر — لا يفقد سطرًا بحكم التعريف |
| **فيه إيداع غير موجود في `main`** (`ahead > 0`) | **76** | موسوم بالكامل ✅ — لا يُحذف إلا بتوقيع |
| **الإجمالي (أسماء فريدة، بلا `main`)** | **321** | — |

مراجع خام: ١٧٥ محليًا · ٢٦٧ بعيدًا (تتقاطع بالأسماء).

### فارق عن التوقيع

التوقيع الوارد: **٣١٢ مدموجة / ٩٤ متجاوزة / ٢٦ محمية = ٤٣٢**. المقيس: **٢٤٥ / ٧٦ / ٣٢١**.

**الفارق الحرج: ٦٧ فرعًا.** لو حُذفت ٣١٢ بوصفها «مدموجة»، لكان من بينها ٦٧ فرعًا يحمل إيداعات غير موجودة في `main` — خرقٌ مباشر للقاعدة الحاكمة في نفس الأمر.

**الخطر مُبطَل الآن:** الوسم الشامل للـ٧٦ سبق أي حذف، فأي خطأ تصنيف لاحق قابل للاستعادة الكاملة.

---

## ١) المدموج بالكامل — 245 فرعًا

`ahead = 0` مقابل `main`: كل إيداعاتها موجودة في الجذع. حذفها لا يفقد شيئًا، وهي الكتلة الكبرى للتنظيف.

**الحالة: مؤجَّلة بأمرك** («الجرد والوسوم أولًا والحذف الجماعي يقبل التأجيل»).

منها فروع `integration/wave1…wave6-staging` و`chore/micro-wave-rule` — أي أن **«فرعَي wave» في توقيعك مدموجان أصلًا** ويسقطان في هذه الكتلة بلا حاجة إلى وسم.

---

## ٢) المحمي — 3 فروع · لا تُمس حرفيًا

| الفرع | الوجود | إيداعات | السبب |
|---|---|---|---|
| `fix/audit-cleanup-wave` | محلي فقط | 7 | موقوف بقرار |
| `claude/p31-muscle-lib-8w7upl` | بعيد | 1 | رأس **PR #7** — خريطة العضلات ❄️ مجمّدة بانتظار الدراسة المقارنة |
| `rescue/legal-data-region-japan` | محلي فقط | 1 | موقوف بقرار |

⚠️ **PR #7 مغلق على GitHub بالفعل** (`CLOSED`) — بينما الميثاق يقول «لا يُدمج ولا يُغلق». الفرع والوسم سليمان، لكن حالة الـPR تخالف المدوَّن. **يُرفع لك.**

---

## ٣) المتجاوز — 23 فرعًا · موسوم، بانتظار توقيع الحذف

أنماط `rescue/*` · `integration/*` · `codex/*` · `wip/*` — شغلها دخل بصيغة أخرى.

أكبرها: `rescue/codex-qimmah-rc-integration` (9) · `rescue/audit-cleanup-wave-wip` (8) · `rescue/codex-structure-uiux-v1` (6) · `rescue/stash-*` (3 لكلٍّ).

`codex/ui-polish` خرج من هذه القائمة: **أُرشف وأُغلق PRه #55** (الحرف هـ) ✅

---

## ٤) الحي غير المدموج — 50 فرعًا · **يحتاج توقيعك**

هذه هي القائمة التي طلبتها. كلها موسومة `archive/<الاسم>` ومدفوعة، فالتوقيع بلا مخاطرة.

### أ. الأثقل — شغل حقيقي غير مدموج

| الفرع | إيداعات | آخر عمل | ملاحظة |
|---|---|---|---|
| `feature/p25-steps-health` | 6 | 2026-06-30 | **PR #4 مفتوح** — الميثاق: «رأسه مدموج في الجذع أصلًا» |
| `backup/claude-p25-3d-20260725` | 6 | 2026-07-25 | نسخة أمان مجسّم 3D |
| `fix/founder-qa-round1` | 5 | 2026-07-18 | جولة QA المؤسس |
| `fix/design-fidelity-v21` | 5 | 2026-07-18 | مطابقة تصميم |
| `feature/phase1-training-engine` | 4 | 2026-06-29 | — |
| `audit/design-fidelity` | 4 | 2026-07-18 | — |

### ب. حارة E — PRات مفتوحة الآن

`e/settings-clarity-r2` (3، **PR #47**) · `e/settings-clarity` (3، **PR #39**) · `e/settings-adopt` (3) · `e/personalization-guardrail-r2` (2، **PR #46**) · `e/personalization-guardrail` (2، **PR #38**)

⚠️ **حذف هذه يترك PRات مفتوحة بلا فروع.** تُحسم مع مصير PRات حارة E (#42–#47) لا منفردة.

### ج. الباقي — 39 فرعًا بإيداع أو إيداعين

`research/v2-*` (5) · `feature/v2-*` (5) · `fix/*` من 2026-06-27 (6) · `claude/q1*` و`claude/p14-*` (5) · `hotfix/*` (2) · `design/v21-slice*` (2) · وغيرها.

أغلبها من **27–30 يونيو** — طبقة ما قبل التوحيد، ومرشّحة قوية للأرشفة+الحذف.

---

## بنود تحتاج قرارك

1. **الحذف الجماعي للـ245 المدموجة** — جاهز للتنفيذ فور توقيعك.
2. **الحذف للـ23 المتجاوزة** — موسومة، جاهزة.
3. **الـ50 الحية** — أعلاه، مقسومة ثلاثًا.
4. **`feat/onboarding-magic` غير موجود** — لا محليًا ولا بعيدًا (ولا `a12676e`/`56bc4c1` في المستودع). CTO-77 لم يصل `origin`.
5. **«الفرع المتبقي»** — لم أستطع تحديده: تصنيفي (٢٤٥/٧٦) لا يطابق تصنيف توقيعك (٣١٢/٩٤/٢٦)، فلا سبيل لاشتقاق أيّ فرع تقصد. القائمة أعلاه كاملة — سمِّه منها.
6. **PR #7** — مغلق فعليًا خلافًا للمدوَّن في الميثاق.
7. **حماية `main`** — غير متاحة عبر API: المستودع خاص على خطة مجانية (HTTP 403). الخطوات أدناه.

---

## حماية `main` — خطوات المؤسس

GitHub يمنع حماية الفروع للمستودعات **الخاصة** على الخطة المجانية:

```
HTTP 403: Upgrade to GitHub Pro or make this repository public to enable this feature.
```

ثلاثة مسارات:

| المسار | الأثر |
|---|---|
| ترقية إلى **GitHub Pro** | حماية كاملة مع بقاء المستودع خاصًا |
| جعل المستودع **عامًا** | حماية مجانية — لكن الكود يصير مكشوفًا |
| **البقاء بلا حماية** | الاعتماد على الانضباط: لا `--force` ولا حذف `main` |

بعد الترقية أو الإعلان:
**Settings → Branches → Add branch protection rule** · Branch name pattern: `main` · فعّل **Do not allow force pushes** و**Do not allow deletions**.

أو بأمر واحد:

```bash
gh api -X PUT repos/km5g98str4-commits/gym-os-template/branches/main/protection -f required_status_checks=null -F enforce_admins=true -f required_pull_request_reviews=null -f restrictions=null -F allow_force_pushes=false -F allow_deletions=false
```

---

## الاستعادة من أي وسم

```bash
git fetch origin --tags
git checkout -b <اسم-جديد> archive/<الاسم>
```

ونسخة كاملة خارج GitHub: `~/qimmah-backup-20260804-pre-cleanup.bundle` (142MB · `git bundle verify` → "complete history").

---

## الختام — 2026-08-04

**نُفِّذ بتوقيع المؤسس:** حُذف **264 فرعًا** (241 مدموجًا بالكامل + 23 متجاوزًا موسومًا) — 216 عن بُعد في ست دفعات، و58 محليًا. `origin` نزل من **267 فرعًا إلى 51**.

**استُثني بقرار أمان معلَن — لا صمتًا:**
- **رؤوس الـ9 PRات المفتوحة**، منها **خمسة مدموجة بالكامل** (#4 · #42 · #43 · #44 · #45) كانت ستُحذف بحكم `ahead=0` فتُغلق PRاتها تلقائيًا.
- **المحمية الثلاث** (§2 أعلاه) — لم تُمس حرفيًا.
- **الـ50 الحية** — بانتظار توقيع بند-ببند.

**عالق بحاجز مادي:** 97 فرعًا محليًا محجوزًا بـworktrees حيّة (`cannot delete branch 'X' used by worktree at '…'`). `git worktree prune` حرّر 5 سجلات مفقودة فقط؛ الباقي مجلدات قائمة على القرص، **وحذفها فعل تدمير خارج التوقيع** فلم يُنفَّذ.

**إعادة التسمية:** `gym-os-template` → **`qimmah`**. التحويل التلقائي **مُختبَر لا مُفترَض**: `git ls-remote` على الرابط القديم يعيد نفس `HEAD` (`c0751e7`)، وAPI يحوّل إلى `km5g98str4-commits/qimmah`. حُدِّث `remote` في `qimmah-deploy`؛ **38 نسخة محلية أخرى** تعمل بالتحويل وتحديثها نظافة لا ضرورة.

**باقٍ بيد المؤسس:** حماية `main` (تحتاج Pro أو مستودعًا عامًا) · مصير الـ50 الحية · إزالة الـ97 worktree · تناقض حالة PR #7.

# UI-AUDIT.md — تشخيص واجهة قِمّة

> **[CTO-72] · المرحلة أ.** قراءة وتوثيق فقط — **لا تعديل في هذه المرحلة**.
> الأساس: `main` @ `0cb434a` (مطابق لـ`origin/main`). التاريخ: 2026-08-03.

---

## 0. كيف قِيس هذا التشخيص

> **قِس قبل أن تصلح.** كل رقم هنا مأخوذ من التطبيق يعمل، لا من قراءة كود.

- **البيئة:** `npm run dev` على `localhost:5199`، Chrome، عرض إطار **1512 CSS px** — والعمود الفعلي للتطبيق `max-w-md` ≈ **448px** (نفس عرض الجوال). الطيّة قِيست عند **750px** ارتفاعًا (المساحة المرئية النموذجية على جوال).
- **الشخصية:** ضيف جديد تمامًا (`localStorage` مُفرَّغ) → «كمّل كضيف» → إعداد كامل (٢٨ سنة · ذكر · ١٧٨سم · ٨٢كجم · خسارة دهون · ٤ أيام · ٤٥ د · نادي · مزيج).
- **عدّاد «العناصر المتنافسة»** سكربت واحد يُشغَّل على كل شاشة، يعدّ العناصر **الحاملة لانتباه مستقلّ** داخل الطيّة الأولى: كل عنصر تحكّم (`button`/`a`/`input`/`select`)، وكل عنوان (`h1`–`h6`)، وكل **نصّ ورقي** (عقدة نصّ بلا أبناء)، وكل أيقونة/صورة — بعد استبعاد المخفي (`display/visibility/opacity`) وإزالة التكرار بالموضع.
  السكربت محفوظ حرفيًا في §7 حتى يُعاد تشغيله ويُقارَن بعد المرحلة ٢.

### ⚠️ حدّان معلنان على هذا القياس

1. **لم يُقَس على جهاز فعلي.** إطار المتصفح ليس جوالًا: `--safe-top/--safe-bottom` صفر هنا، وسلوك لوحة المفاتيح والتمرير المطاطي غير ممثَّل. كل ما يخصّ iOS يبقى **استنتاجًا لا حقيقة مؤكّدة** حتى يُفحص على الجهاز.
2. **الحالة الفارغة وحدها.** الشخصية بلا تاريخ (لا تمارين ولا وجبات ولا قياسات). الشاشة الممتلئة **لم تُقَس** — وبعض ما يبدو هنا «فراغًا بليدًا» يصير محتوى نافعًا بعد أسبوع استخدام. مميَّز في كل بند.

### ملفات محلية غير ملتزَم بها وقت هذا التشخيص (§2 من الميثاق — لا تُلمس، وتُوثَّق)

- `docs/audit/QIMMAH-FULL-AUDIT-2026-08-03.md` (معدَّل، +275/−…)
- `docs/audit/QIMMAH-PROPOSED-FIXES-2026-08-03.md` (معدَّل)

**لم تُلمس ولم تُقرأ كمصدر حقيقة** لهذا التقرير.

---

## 1. لوحة العدّاد — ترتيب الشاشات بالازدحام

| # | الشاشة | العناصر في الطيّة | تحكّم | عناوين | نصوص | **الدرجة** |
|---|--------|------------------:|------:|-------:|-----:|-----------|
| 1 | **الإعداد الأولي — خطوة ١** | **21** | 8 | 1 | 12 | 🟢 نظيفة |
| 2 | **الإعدادات** | **21** | 12 | 5 | 4 | 🟡 مزدحمة |
| 3 | **التقدّم** | **23** | 6 | 2 | 15 | 🟡 مزدحمة |
| 4 | **الملف الشخصي** | **25** | 6 | 2 | 17 | 🟡 مزدحمة |
| 5 | **التمارين (التبويب)** | **31** | 12 | 4 | 15 | 🟡 مزدحمة |
| 6 | **التغذية** | **38** | 6 | 2 | 30 | 🔴 فوضوية |
| 7 | **اللوحة / اليوم** | **40** | 10 | 4 | 26 | 🔴 فوضوية |
| 8 | **معالج التعديل** | **44** | 18 | 2 | 24 | 🔴 فوضوية |
| 9 | **شاشة التمرين المفتوح** | **48** | 17 | 4 | 27 | 🔴 فوضوية |
| 10 | **مكتبة التمارين / البحث** | **58** | 38 | 2 | 18 | 🔴 فوضوية |

> **مرجع المؤسس من الميدان:** «شاشة التمرين المفتوح فيها 13+ عنصرًا متنافسًا».
> **القياس يؤكّده ويزيد:** ٤٨ عنصرًا في الطيّة الأولى وحدها.

---

## 2. أربعة أعطال قِيست بالبرهان — **تسبق كل نقاش تصميمي**

> هذه ليست «ازدحامًا». هذه **أشياء لا تعمل**، ثبتت بـ`getBoundingClientRect` و`elementFromPoint` لا بالنظر.

### 🔴 ع-١ — شريط «ثبّت التطبيق» يغطّي زرّ إنهاء الإعداد

`InstallPrompt` (`src/components/InstallPrompt.tsx:56`) شريط `fixed bottom-0 z-[60]`. ومعالج الإعداد `z-50`. فالشريط **فوقه دائمًا**.

```
شاشة «خطتك جاهزة»:
  زرّ «الدخول للوحة»      → top 747 · bottom 799
  شريط التثبيت           → top 708 · bottom 827 · z-index 60
  document.elementFromPoint(مركز الزرّ) = «أضِفه لشاشتك الرئيسية عشان يشتغل…»
```

**الزرّ الذي يُدخِل المستخدم التطبيق غير قابل للنقر** ما دام الشريط ظاهرًا. ونفس التغطية على **زرّ «التالي»** في كل خطوة من خطوات الإعداد الخمس (قِيست على الخطوة ١: زرّ 759→811 · شريط 708→827).

**ملاحظة صدق:** في رأس الملف نفسه تعليق يقول **«لا تحجب الواجهة أبدًا»** — والقياس ينقضه. تعليق يصف نيّة لا واقعًا.

**النطاق:** أندرويد/Chrome (بعد `beforeinstallprompt`) وiOS Safari. **لا يظهر** داخل غلاف Capacitor (`isNativePlatform()`).
**المخرج القائم:** زرّ «لاحقًا» يُغلق الشريط نهائيًا — أي أن المستخدم **يستطيع** الخروج من الفخّ، لكنه يواجهه أولًا.

### 🔴 ع-٢ — شريط «التمرين التالي» يغطّي أزرار المجموعة الثالثة

`src/components/WorkoutMode.tsx` — الشريط السفلي الثابت يعلو محتوى المحرّر:

```
زرّ «+» (تكرارات · الجولة 3)  → 165,770 · elementFromPoint = «التمرين التالي»  ← مغطّى
زرّ «+» (وزن   · الجولة 3)  → 762,770 · elementFromPoint = «التمرين التالي»  ← مغطّى
(أزرار الجولتين ١ و٢ سليمة — elementFromPoint يعيد الزرّ نفسه)
```

هذا **هو بعينه** ما وصفه المؤسس بـ«زرّ + عائم يركب على زر التمرين التالي» — مرئيًّا من الجهة الأخرى.

### 🔴 ع-٣ — `bg-primary-soft/N` لا يُنتج خلفية إطلاقًا (٥ مواضع)

`.bg-primary-soft` أداة **مكتوبة يدويًا** في `src/styles/index.css:308` داخل `@layer components` — **وليست لونًا في `tailwind.config.js`**. لذلك `bg-primary-soft/95` ليس صنفًا صالحًا: Tailwind لا يولّد له شيئًا، والصنف اليدوي لا يُطابقه (اسمه في الـHTML فيه `/95`).

**النتيجة المقيسة** على شريط الراحة داخل الجلسة:

```
العنصر: div.fixed.inset-x-0.bottom-[4.75rem].z-20.border-t.border-primary-soft.bg-primary-soft/95.backdrop-blur
getComputedStyle(...).backgroundColor = "rgba(0, 0, 0, 0)"   ← شفاف تمامًا
backdrop-filter = "blur(8px)"                                 ← الضباب وحده باقٍ
```

فتظهر **صور البدائل الفوتوغرافية من خلف الشريط**، والعدّاد والنصّ يجلسان على صورة نادٍ حمراء.

**المواضع الخمسة:**

| الملف | السطر | الصنف |
|-------|------:|-------|
| `src/components/WorkoutMode.tsx` | 696 | `bg-primary-soft/95` (شريط الراحة) |
| `src/components/WorkoutMode.tsx` | 525 | `bg-primary-soft/60` (شارة مجموعة مكتملة) |
| `src/components/InstallBanner.tsx` | 55 | `bg-primary-soft/50` |
| `src/components/DeviceSettings.tsx` | 60 | `bg-primary-soft/40` |
| `src/views/ExerciseLibraryView.tsx` | 203 | `bg-primary-soft/40` |

> `border-primary-soft` **سليم** لأنه يُستعمل بلا شرطة شفافية. العطل في المُعدِّل وحده.

### 🔴 ع-٤ — «ابدأ نظيفًا» يمحو جلسة فيها تقدّم بلا أي تأكيد

إعادة إنتاج مقيسة:

1. ابدأ تمرين اليوم كضيف · أنهِ **مجموعة واحدة** ⇒ `qimmah:activeWorkout:v1` يحمل الجلسة.
2. اضغط **✕** (إغلاق) ⇒ **خروج فوري بلا سؤال**. البيانات **باقية** (بطاقة «عندك تمرين لم ينتهِ» تظهر) — فلا فقدان هنا، لكن لا تأكيد أيضًا.
3. اضغط **«ابدأ نظيفًا»** ⇒ `hasDialog: false` · `qimmah:activeWorkout:v1` = `{}` ⇒ **المجموعة المسجّلة ذهبت نهائيًا. بلا سؤال، بلا تراجع.**

**التمييز مهم:** الخطوة ٢ نقص طمأنة؛ الخطوة ٣ **فقدان بيانات صامت** — وهي المخالفة الحقيقية لـ§2-٣ من دستور الجودة.

---

## 3. الشاشات — شاشة شاشة

### 3.1 اللوحة / اليوم — `TodayV2.tsx` (حيّة)

| | |
|---|---|
| **سؤالها الواحد** | «وين أنا اليوم؟» |
| **العناصر في الطيّة** | **40** (10 تحكّم · 4 عناوين · 26 نصًّا) |
| **الدرجة** | 🔴 **فوضوية** |

**ما يظهر فعلًا للقادم الجديد** (بالترتيب):
ترويسة القشرة → **شريط تثبيت** → تاريخ + تحية → بطاقة «خلّنا نبدأ بشي واحد» → **بطاقة ماكروز بأربع حلقات أصفار** → «وش باقي لك اليوم؟ ٤ مهام» (٤ بطاقات) → «نبض أسبوعك: نحتاج بيانات أكثر» → **رسالة نجاح سفلية تغطّي شريط التنقّل**.

**(٣) التكرارات — الوجه الأثقل هنا:**

| المعلومة | كم مرّة | أين |
|----------|--------:|-----|
| فعل «سجّل ماء» | **٢** | بطاقة أول انتصار «سجّل كوب ماء» · بطاقة «مويتك — سجّل ماء» |
| فعل «سجّل وجبة» | **٢** | «سجّل عشاك» · «سجّل أول وجبة — سجّل وجبة» |
| **رقم السعرات** | **٣** | `0/2,207 كالوري` · حلقة `سعرات 0 / 2,207 متبقي` · `٠ من ٢٬٢٠٧ سعرة` |

**(٦) كسر لغة بصرية — نظاما أرقام على شاشة واحدة:**
`0/2,207 كالوري` **لاتينية** بينما `٠ من ٢٬٢٠٧ سعرة` **هندية** — **نفس الرقم، بخطّين مختلفين، بفارق تمريرة واحدة**.

**(٤) الحالات الفارغة البليدة:** أربع حلقات ماكرو بحجمها الكامل تعرض `0`/`0`/`0`/`0`، وبطاقة «نبض أسبوعك» بحجم بطاقة ممتلئة لتقول «ما فيه بيانات».

**(٥) تصادمات:** شريط التثبيت أعلى المحتوى + `SuccessToast` أسفله يغطّي شريط التنقّل لحظة الدخول.

---

### 3.2 التمارين (التبويب) — `WorkoutView.tsx` (v1، حيّة)

| | |
|---|---|
| **سؤالها الواحد** | «أبدأ تمرين اليوم» |
| **العناصر في الطيّة** | **31** |
| **الدرجة** | 🟡 **مزدحمة** |

**(٣) التكرارات:**
- **الكلمة نفسها مرّتين متتاليتين:** eyebrow «تمرين» ثم `h1` «تمرين».
- **ثلاثة عناوين للشاشة الواحدة:** «التمارين» (ترويسة القشرة) · «تمرين» (eyebrow) · «تمرين» (`h1`).
- **فعل «ابدأ تمرين اليوم» مرّتان:** بطاقة «بدء سريع» البرتقالية، ثم بطاقة «تمرين اليوم» داخل «خطتي» بزرّ «ابدأ».

**(٦) كسر لغة بصرية:** أرقام لاتينية حصرًا هنا (`اليوم 2`، `5 تمارين`، `4 أيام/أسبوع`) — بينما الملف الشخصي عربي-هندي (`٠ تمرين`، `الأسبوع ١ من ٨`). **الأرقام تتبدّل بتبديل التبويب.**

**(٥) قصّ نصوص:** «اليوم 2 · سفلي · 5 تما…» و«… جهاز ركـ…».

---

### 3.3 شاشة التمرين المفتوح — `WorkoutMode.tsx` (v1، حيّة) · **الشاشة المرجعية**

| | |
|---|---|
| **سؤالها الواحد** | «وش أسوي الحين؟» |
| **العناصر في الطيّة** | **48** (17 تحكّم · 4 عناوين · 27 نصًّا) |
| **الدرجة** | 🔴 **فوضوية — الأسوأ وظيفيًا** |

**كل ما وصفه المؤسس، مؤكَّدًا واحدًا واحدًا:**

| ادّعاء الميدان | الحالة | البرهان |
|----------------|--------|---------|
| «13+ عنصرًا متنافسًا» | ✅ **مؤكَّد وأزيد** | ٤٨ في الطيّة |
| «‹1 من 2› مكررة مرتين» | ✅ **مؤكَّد** | نصّ الصفحة: `1 من 2` تحت عنوان الترويسة، و`1 من 2` شارة بجانب اسم التمرين |
| «اسم التمرين مكرر» | ✅ **مؤكَّد** | «هاك سكوات جهاز / Hack Squat Machine» مرسوم **داخل الصورة** ثم مكتوب `h1` تحتها |
| «بطاقتان لرسالة فارغة واحدة» | ✅ **مؤكَّد** | «آخر مرة → ما فيه سجل سابق» و«أفضل أداء → ما فيه سجل سابق» — **نفس الجملة حرفيًا، بطاقتان متجاورتان** |
| «زر + عائم يركب على زر التمرين التالي» | ✅ **مؤكَّد** | ع-٢ أعلاه |
| «صور بدائل فوتوغرافية تكسر لغة الرسوم» | ✅ **مؤكَّد** | التمرين الأساسي **رسم خطّي** على تدرّج داكن؛ البديلان **صورتان فوتوغرافيتان** لصالة حقيقية، جنبًا لجنب |

**ويُضاف من القياس:**
- **ع-٣:** شريط الراحة بلا خلفية ⇒ الصور الفوتوغرافية تخترقه.
- **ع-٤:** لا تأكيد عند الخروج، وفقدان صامت عند «ابدأ نظيفًا».
- حقل «الوزن (كجم)» يبدأ **فارغًا تمامًا** — لا صفر ولا اقتراح ولا «—».
- كل جولة تحمل ٨ عناصر (تسمية · هدف · حقلان × زرّان لكل حقل · زرّ «تم») × ٣ جولات = **٢٤ عنصرًا لتمرين واحد**.

---

### 3.4 التغذية — `NutritionView.tsx` (v1، حيّة)

| | |
|---|---|
| **سؤالها الواحد** | «وش آكل / وش سجّلت؟» |
| **العناصر في الطيّة** | **38** (منها **30 نصًّا** — أعلى كثافة نصّية في التطبيق) |
| **الدرجة** | 🔴 **فوضوية** |

**(٣) التكرارات — الشاشة الأسوأ في هذا الباب:**

| المعلومة | كم مرّة | الصيغ |
|----------|--------:|-------|
| السعرات | **٣** | معادلة `2207 احتياجك − 0 الأكل + 0 التمرين = 2207 المتبقّي` · شريط `سعرات 0 / 2207` · سطر `سعرات متبقّية: 2207` |
| البروتين | **٣** | حلقة `0 / 148غ` · شريط `بروتين 0 / 148غ` · سطر `بروتين متبقّي: 148غ` |
| الماء | **٢** — **وبوحدتين** | حلقة `0 / 3000مل` · قسم `0.00 / 3.0 لتر` |
| تنويه «القيم الغذائية تقريبية…» | **٢** | مرّة بعد «سجل اليوم»، ومرّة بعد قسم الماء |

**(٥) تصادم مقيس — النصّ يخرج من بطاقته:** تسميات الحلقات الأربع كلّها تتجاوز حدود حاويتها:

```
"/ 3000مل"  العنصر 534.4→574.5   الحاوية 566→574.5   ← خروج 31.6px
"/ 255غ"    العنصر 760.5→787.5   الحاوية 779→787.5   ← خروج 18.5px
"/ 148غ"    العنصر 867.0→894.0   الحاوية 885.5→894   ← خروج 18.5px
"/ 66غ"     العنصر 660.6→681.0   الحاوية 672.5→681   ← خروج 11.9px
```

**(٦) كسور لغة بصرية:**
- تسميات الحلقات مقصوصة إلى **حرف واحد ونقطة**: `ب.` `ك.` `د.` `م.` — غير مفهومة بلا تخمين.
- `0.00 / 3.0 لتر` — منزلتان عشريتان لكوب ماء.
- أرقام لاتينية حصرًا، بخلاف الملف الشخصي.

---

### 3.5 التقدّم — `ProgressView.tsx` (v1، حيّة)

| | |
|---|---|
| **سؤالها الواحد** | لا يوجد واحد — تجيب **سبعة** (وزن · سلسلة · حجم · أرقام قياسية · عضلات · مجسّم · خطوات · تذكيرات) |
| **العناصر في الطيّة** | **23** |
| **الدرجة** | 🟡 **مزدحمة** — و🔴 في الحالة الفارغة |

**(٤) الحالات الفارغة البليدة — الشاشة المرجعية لهذا العيب:**
**سبع بطاقات بحجمها الكامل، كلّها فارغة.** وبينها **نصّ فراغ مكرَّر حرفيًا**:

> «ما فيه تمارين مسجّلة لسا. ابدأ تمرينك وبيظهر تقدّمك هنا.»
> تظهر مرّتين: تحت «حجم التمرين» وتحت «العضلات هذا الأسبوع».

ومعها: مجسّم عضلات كامل بأربعة أزرار زوايا يقول «ابدأ تمرينك وبتشوف عضلاتك تتلوّن هنا»، وشبكة سبعة أيام فارغة، وعدّاد خطوات `0` / `0%` / `باقي 10,000 خطوة`.

> ⚠️ **تحفّظ معلن:** هذه شاشة **بطبيعتها** تعيش على التاريخ. جزء من «البلادة» هنا ثمن الحالة الفارغة لا خلل تصميم. **ما لا عذر له** هو تكرار الجملة نفسها حرفيًا، وتساوي حجم البطاقة الفارغة مع الممتلئة.

**(٦)** `10,000` لاتينية و`٣٦٠°` هندية — **على نفس الشاشة**.

---

### 3.6 الملف الشخصي — `ProfileV2.tsx` (حيّة)

| | |
|---|---|
| **سؤالها الواحد** | «مين أنا في التطبيق، ووين أعدّل؟» |
| **العناصر في الطيّة** | **25** |
| **الدرجة** | 🟡 **مزدحمة** |

**(٣) تكرار من نوع آخر — اسمان للشاشة الواحدة فوق بعض:**
ترويسة القشرة تقول **«ملفك»**، وأول عنوان في المحتوى يقول **«حسابي»**. سطران متلاصقان، اسمان مختلفان، شاشة واحدة.

**(٤) فراغ مضاعف:** ثلاث بطاقات `٠ تمرين` / `٠ أيام متتالية` / `٠ أرقام قياسية` — **وتحتها مباشرة** «نحتاج بيانات أكثر — أكمل تمرينك الأول.» أي أن الأصفار والجملة **تقولان الشيء نفسه**؛ إحداهما تكفي. ثم شبكة «الالتزام · آخر ١٠ أسابيع» عشرة مربّعات فارغة + «لا تمارين مسجّلة بعد».

**(٩) ما يحتاج قرارًا:** سطر **«قِمّة+ · خطط وتحليلات أعمق — اعرف المزيد»**. الميثاق يقول «ليس قالبًا تجاريًا يُباع» — وهذا عرض اشتراك. **يُرفع ولا يُحسم اجتهادًا** (§7 من دستور الجودة).

---

### 3.7 الإعدادات — `SettingsView.tsx` (حيّة)

| | |
|---|---|
| **سؤالها الواحد** | «وين أغيّر شيئًا؟» |
| **العناصر في الطيّة** | **21** |
| **الدرجة** | 🟡 **مزدحمة** |

**(٦) كسر لغة بصرية — الأوضح في التطبيق:** هذه الشاشة **خارج `MobileShell`**، فتتمدّد على **1512px كاملة** بينما كل تبويب آخر محبوس في عمود ≈448px. الانتقال من الملف الشخصي إلى الإعدادات **يقفز من عمود جوال إلى صفحة سطح مكتب**.

**(٣) تكرار — أربعة أسطح تثبيت في تطبيق واحد:**
1. `InstallBanner` أعلى كل تبويب.
2. `InstallPrompt` شريط سفلي (ع-١).
3. بطاقة «ثبّت قِمّة على جهازك → تثبيت التطبيق» هنا.
4. قسم «ثبّت التطبيق» بدليل مرقّم خطوة-خطوة هنا أيضًا.

**(٩) ما يحتاج قرارًا:** «إعادة ضبط البيانات» — أخطر فعل في التطبيق — يمرّ عبر `window.confirm` خام (`SettingsView.tsx:128`)، بينما حذف الحساب له حوار داخل التطبيق بتصميمه. نفس ملاحظة `[QA-39]` في `qa-reports/QA-SESSION-2-room-B.md`، **ما زالت قائمة**.

---

### 3.8 مكتبة التمارين / البحث — `ExerciseLibraryView.tsx` (حيّة)

| | |
|---|---|
| **سؤالها الواحد** | «ألقى تمرينًا» |
| **العناصر في الطيّة** | **58** (منها **38 عنصر تحكّم**) |
| **الدرجة** | 🔴 **فوضوية — الأكثف في التطبيق** |

**(٢) الازدحام:** ٢٤ رقاقة تصفية (١٢ عضلة + ١٢ معدّة) مفتوحة كلّها افتراضيًا قبل أن يكتب المستخدم حرفًا.

**(٦) كسور لغة بصرية:**
- **ثلاث رقاقات إنجليزية داخل صفّ عربي:** `band` · `plate` · `rope` — بينما جاراتها «بار» و«مقعد» و«كيبل» و«دمبل».
- **خلط الوسائط داخل الشبكة نفسها:** بعض البطاقات صور فوتوغرافية حقيقية (أرجحة الكيتل بل · أعظم تمدّد · الحشرة الميتة)، وأخرى أيقونة/رسم بديل (أرجحة الأرجل · أصابع للبار · الإليبتيكال) — **صفّان متجاوران بلغتين بصريتين**.
- **ثلاثة عناوين:** «التمارين» (القشرة) · «المكتبة» (eyebrow) · «مكتبة التمارين» (`h1`).
- قصّ في كل بطاقة تقريبًا: «أعظم تمدّد (…» · «…reatest Stretch».
- `181` لاتينية.

---

### 3.9 معالج التعديل — `CustomizationCenter` (حيّة عبر `#/setup` بعد اكتمال الإعداد)

| | |
|---|---|
| **سؤالها الواحد** | «أعدّل خطتي» |
| **العناصر في الطيّة** | **44** (18 تحكّم) |
| **الدرجة** | 🔴 **فوضوية — وأخطرها تناقضًا** |

**(٦) كسر لغة بصرية:** **تخطيط عمودين لسطح المكتب** (نموذج يسارًا · «معاينة حية» يمينًا) داخل تطبيق جوال. لا شيء في قِمّة يشبه هذه الشاشة.

**(٣) التكرار:** التقدّم معروض **ثلاث مرّات**: `الخطوة 1 من 4: بياناتك` · `25%` · شريط تقدّم · وأربع رقاقات مرقّمة. أربعة تمثيلات لمعلومة واحدة.

**(٩) تناقض حقيقي يحتاج قرارًا — نموذجا أهداف في تطبيق واحد:**

| | الإعداد الأولي (`OnboardingV2`) | معالج التعديل (`CustomizationCenter`) |
|---|---|---|
| **الأهداف** | ٣: خسارة دهون · ثبات وتحسين عام · بناء عضل | **٥**: تنشيف · تضخيم · محافظة على العضل · رجوع بعد انقطاع · صحة عامة |
| **الجنس** | ٢: ذكر · أنثى | **٣**: ذكر · أنثى · غير محدّد |
| **الصياغة** | واعية بالمستوى (مبتدئ يرى «خسارة دهون») | **مصطلح صالة ثابت** («تنشيف») مهما كان المستوى |

المستخدم يختار «خسارة دهون» في الإعداد، ثم يفتح التعديل فيجد نفسه على **«تنشيف»** ضمن قائمة من خمسة لم يرها قط. وصياغة المستوى — التي بُنيت عمدًا في `onboardingIntent.ts` — **تسقط تمامًا** هنا.

**(٩) بقايا لغة القالب — خارج ما يحرسه `test:no-template-language`:**
- زرّ **«معاينة في الموقع»**
- عنوان **«معاينة خطتك»**
- تسمية **«صاحب الخطة»**
- **بطاقة معاينة بأربعة أصفار** (`0 تمارين` · `0 وجبات` · `0 مكملات` · `0 قياسات`) **ومربّعا لون للعلامة** — هذه واجهة **مخصِّص قالب**، لا محرّر خطة رياضي.

> الحارس القائم يفحص «صفحتك»/"your page" فقط. **«معاينة في الموقع» و«صاحب الخطة» تعبران منه.**

---

### 3.10 الإعداد الأولي — `OnboardingV2.tsx` (حيّة)

| | |
|---|---|
| **سؤالها الواحد** | «سؤال واحد لكل خطوة» |
| **العناصر في الطيّة (خطوة ١)** | **21** — **الأنظف في التطبيق** |
| **الدرجة** | 🟢 **نظيفة** بنيويًا · 🟡 في اتّساق السياق |

**ما هو صحيح:** خطوة = شاشة، عدّاد واضح، شريط مقسّم، زرّ واحد أساسي، تحقّق داخلي معلَن، والموافقة الصحية **قبل** أي حقل.

**(٣) التكرار — ثلاثة أسطر «ليش نسأل» في خطوة واحدة (الأساسيات):**
1. `subtitle`: «أربع أرقام ونقدّر احتياجك اليومي.»
2. `genderNote`: «ندخله بس في حساب الطاقة.»
3. `whyNote` **في الأسفل**: «نستخدمها عشان نقدّر سعراتك وماكروزك…»

**(٢) وعدم الاتّساق — السياق يختلف من خطوة لأخرى:**

| الخطوة | سطر السياق | موضعه |
|--------|-----------|-------|
| ١ الأساسيات | ✅ ثلاثة | مبعثرة (أعلى · وسط · أسفل) |
| ٢ النية والمستوى | ✅ واحد | أعلى |
| ٣ **الهدف** | ❌ **لا شيء** | — (عنوان فقط، وملاحظة «تقدر تغيّره» في الأسفل) |
| ٤ التدريب | ✅ واحد | أعلى |
| ٥ المعدّات | ✅ واحد | أعلى |

**(٥) تصادم:** ع-١ — شريط التثبيت يغطّي «التالي» في كل خطوة.
**(٢) ازدحام:** خطوة ٢ تحمل **سؤالين** (نية + مستوى) = ٦ بطاقات اختيار كبيرة، وهي أطول خطوة في التدفّق.

---

## 4. أنماط عابرة للشاشات

### ن-١ · **نظاما أرقام في تطبيق واحد** — أوسع كسر بصري

| هندية `٠١٢٣` | لاتينية `0123` |
|---|---|
| الملف الشخصي · الإعداد الأولي · بطاقات «باقي لك اليوم» | التغذية · التقدّم · تبويب التمارين · المكتبة · معالج التعديل · بطاقة ماكروز اللوحة |

وتلتقيان **على شاشة واحدة** في موضعين على الأقل: اللوحة (`0/2,207` مع `٠ من ٢٬٢٠٧`) والتقدّم (`10,000` مع `٣٦٠°`).

### ن-٢ · ثلاثة عناوين لكل شاشة
`MobileShell` يرسم عنوانًا، ثم تكرّره الشاشة بـeyebrow + `h1`. أوضحها: «التمارين / تمرين / تمرين».

### ن-٣ · الأصفار تُعرض بحجم المحتوى
لا يوجد **حجم بطاقة للحالة الفارغة**. بطاقة تقول «لا شيء» تحجز نفس مساحة بطاقة تحمل أسبوعًا من العمل.

### ن-٤ · الشاشة الحيّة ليست الشاشة المصقولة
`WorkoutV2.tsx` (1385 سطرًا) · `NutritionV2.tsx` (537) · `ProgressV2.tsx` (594) — **يتيمة بلا مستورد** (مُعلَن في `scripts/run-analytics-proof.mjs:105`). كل صقلٍ لاحق يجب أن يقع على `WorkoutView`/`WorkoutMode`/`NutritionView`/`ProgressView`، لا على نظائرها V2.

---

## 5. ما لم يُفحص — لئلا يُقرأ الصمت اكتمالًا

- **جهاز فعلي (iOS)** — المنطقة الآمنة، لوحة المفاتيح، الإشعارات، التمرير المطاطي.
- **الوضع الفاتح** — كل القياسات هنا في الوضع الداكن.
- **الإنجليزية / LTR** — لم تُمشَ الشاشات بها.
- **الشاشة الممتلئة** بعد أسبوع بيانات حقيقية.
- **تباين AA بالأرقام** — لم يُحسب في هذه الجولة (بند ٤ من دستور الجودة).
- **`LoginView` · `RecoveryView` · `MyStatsView` · `CalcExplainerView` · `StepsView`** — خارج قائمة المرحلة أ.

---

## 6. ما يحتاج قرار المؤسس (لا يُحسم اجتهادًا — §7)

| # | البند | لماذا يُرفع |
|---|-------|------------|
| ق-١ | **ع-١ · شريط التثبيت يغطّي زرّ إنهاء الإعداد** | عطل حاجب خارج البنود الستة. أُصلح فورًا في هذه الموجة أم يُجدول للمرحلة ٢؟ |
| ق-٢ | **نموذجا أهداف متناقضان** (٣ مقابل ٥) | قرار منتج لا تصميم: أيّهما مصدر الحقيقة؟ |
| ق-٣ | **بقايا لغة القالب في معالج التعديل** | «معاينة في الموقع» · «صاحب الخطة» · بطاقة الأصفار ومربّعا اللون. حذف أم إعادة صياغة؟ |
| ق-٤ | **«قِمّة+»** في الملف الشخصي | ظاهره تعارض مع «ليس قالبًا تجاريًا يُباع». |
| ق-٥ | **توحيد نظام الأرقام** | هندية أم لاتينية؟ قرار هوية يمسّ كل شاشة. |
| ق-٦ | **`window.confirm` لإعادة ضبط البيانات** | `[QA-39]` ما زال مفتوحًا. |

---

## 7. سكربت القياس — للمقارنة بعد المرحلة ٢

> يُلصق في console المتصفح على أي شاشة، ثم `__fold()`.

```js
window.__fold = (vh = 750) => {
  const root = document.querySelector('#main-content') || document.body
  const items = [], seen = new Set()
  root.querySelectorAll('*').forEach((el) => {
    const r = el.getBoundingClientRect()
    if (r.top >= vh || r.bottom <= 0 || !r.width || !r.height) return
    const cs = getComputedStyle(el)
    if (cs.visibility === 'hidden' || cs.display === 'none' || cs.opacity === '0') return
    const tag = el.tagName
    let kind = null
    if (['BUTTON', 'A', 'INPUT', 'SELECT'].includes(tag)) kind = 'control'
    else if (/^H[1-6]$/.test(tag)) kind = 'heading'
    else if (tag === 'SVG' || tag === 'IMG') kind = 'icon'
    else if (['P', 'SPAN', 'LI', 'LABEL'].includes(tag) && !el.children.length && el.textContent.trim()) kind = 'text'
    if (!kind) return
    if (kind !== 'control' && el.closest('button,a')) kind += '-in-control'
    const key = `${kind}|${Math.round(r.top)}|${Math.round(r.left)}|${el.textContent.trim().slice(0, 20)}`
    if (seen.has(key)) return
    seen.add(key)
    items.push({ kind })
  })
  const byKind = {}
  items.forEach((i) => { byKind[i.kind] = (byKind[i.kind] || 0) + 1 })
  return { total: items.length, byKind }
}
```

**كاشف التصادم** (يثبت التغطية لا يستنتجها):

```js
window.__covered = (sel) => [...document.querySelectorAll(sel)].map((b) => {
  const r = b.getBoundingClientRect()
  const top = document.elementFromPoint(r.left + r.width / 2, r.top + r.height / 2)
  return { el: b.getAttribute('aria-label') || b.innerText.trim().slice(0, 20),
           covered: top !== b && !b.contains(top),
           by: top?.closest('button,div')?.innerText?.trim().slice(0, 30) }
})
```

**كاشف الخروج من الحاوية:**

```js
window.__overflow = () => [...document.querySelectorAll('#main-content *')]
  .filter((el) => !el.children.length && el.getBoundingClientRect().width)
  .map((el) => ({ el, r: el.getBoundingClientRect(), p: el.parentElement.getBoundingClientRect() }))
  .filter(({ r, p }) => r.left < p.left - 1 || r.right > p.right + 1)
  .map(({ el, r, p }) => ({ t: el.textContent.trim().slice(0, 20), el: `${r.left}..${r.right}`, parent: `${p.left}..${p.right}` }))
```

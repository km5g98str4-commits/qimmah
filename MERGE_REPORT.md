# MERGE_REPORT.md — عملية «النرد الثابت» [CTO-61]

> تنفيذ قائمة الدمج الموقّعة على فرع تكامل. **الجذع `main` لم يستقبل أي دمج.**
> تاريخ التنفيذ: **2026-08-02** · المنفِّذ: Claude Code · الحالة: **مكتمل — بانتظار [CTO-62]**

---

## 0. الملخص التنفيذي

| المقياس | القيمة |
|---|---|
| فرع التكامل | **`integration/nard-2026-08`** (من `origin/main` = `658d088`) |
| رأس فرع التكامل | `9638adf` |
| **الفروع المدموجة فعلياً من أصل 31 مرشحاً (الموجات 1–4)** | **26 / 31** |
| مدموج إضافي — البنية التحتية (الموجة 5) | **2 / 4** |
| مدموج إضافي — الأعمال المُنقَذة (الموجة 6) | **1** |
| **إجمالي عمليات الدمج على فرع التكامل** | **29** |
| فروع أُوقفت/أُجّلت لقرار المؤسس | **5** (م4) + **2** (م5) + بقية المُنقَذة |
| الصافي مقابل `origin/main` | **203 ملف · +11867 / −612** |
| بوابات الموجات المجتازة | **6 / 6** (`nard/wave-1..6-done`) |
| دمج في `main` · push إلى `origin/main` · إعادة كتابة تاريخ · حذف فرع · حذف stash | **صفر** |

**النتيجة:** فرع تكامل نظيف يبني ويجتاز كل البوابات، جاهز لرحلات e2e الحرجة وبوابة الدمج النهائية [CTO-62].

---

## 1. المرحلة 0 + 1 — مواءمة الجذع وفرع التكامل

- **الملفات غير المتتبَّعة (18):** كانت في الشجرة على `site/launch-pack` (منها 8 نسخ محلية **مطابقة بايت‑ببايت** لملفات مثبّتة على `origin/main`، وكانت ستوقف `checkout`). حُفظت كلها بـ`git stash push -u` (إجراء **إضافي قابل للتراجع**، بلا حذف) — الـstash القديمة المحميّة بقيت سليمة (`stash@{1}`).
- **المرحلة 0:** `git checkout main` ثم `git merge --ff-only origin/main` — نجح التقديم السريع (`b0a7e3f` → `658d088`، الجذع كان متأخراً 584 إيداعاً، سلف نظيف).
- **المرحلة 1:** `git checkout -b integration/nard-2026-08 origin/main` عند `658d088`.
- **خط الأساس (Baseline):** `origin/main` نفسه اجتاز `npm ci · typecheck · build · lint · test:gate` **أخضر بالكامل** — فأي فشل لاحق منسوب للدمج لا للجذع.

---

## 2. البوابات — كل موجة

كل موجة اجتازت (قبل الوسم): `npm ci` → `typecheck` → `build` → `lint` → `test:gate` (≈57 سكربت إثبات). كلها خضراء.

| الموجة | الوسم | الرأس | البوابة |
|---|---|---|---|
| 1 | `nard/wave-1-done` | `e07084b` | ✅ |
| 2 | `nard/wave-2-done` | `040b4fd` | ✅ |
| 3 | `nard/wave-3-done` | `45683ca` | ✅ |
| 4 | `nard/wave-4-done` | `67d1ac1` | ✅ |
| 5 | `nard/wave-5-done` | `a384984` | ✅ |
| 6 | `nard/wave-6-done` | `9638adf` | ✅ |

> **ملاحظة الموجة 4:** ظهر فشل مؤقّت في `test:onboarding-e2e-contract` بعد الدمج الأول للفرع 31، عولج فوراً (انظر §3، الفرع 31)، ثم أُعيدت البوابة خضراء قبل الوسم.

---

## 3. الموجات 1–4 — الـ31 مرشحاً

### الموجة 1 — صفر تعارض (10/10 مدموجة)
`docs/beta-ops` · `audit/code-notes-full` · `audit/v3-fidelity-full` · `audit/v3-inventory` · `audit/app-structure-deep` · `chore/repo-ledger` · `research/product-ideas` · `chore/v3-baseline` · `chore/training-focus-verdict` · `fix/brand-mark-swap`.
> `chore/v3-baseline` صار سلفاً بعد `chore/training-focus-verdict` (محتواه حاضر، لا فقد).

### الموجة 2 — صفر تعارض · كود واختبارات (5/5 مدموجة)
`g/account-isolation` · `origin/claude/h2-active-session-test-gaps` · `i/journey-advanced` · `origin/fix/release-qa-offline-a11y` · `claude/q15-ios-shell-visual-fix`.

### الموجة 3 — تعارض `package.json` (4/4 مدموجة)
حُلّ كل تعارض **باتحاد السكربتات** (لا اختيار طرف) ثم إعادة توليد ملف القفل.

| الفرع | الحل |
|---|---|
| `origin/e/plan-preview-host` | اتحاد `test:gate`: أُضيف `test:e-guardrail` مع إبقاء `test:e-steps`/`test:e-calc-explainer` |
| `claude/p14-english-content-completion` | اتحاد السكربتات. ⚠️ **انحراف موثّق:** اتحاد التبعيات أعاد `react-body-highlighter` (محذوفة من الجذع، §11 مجمّدة، غير مستوردة) — **أُزيلت في إيداع تصحيح مستقل** `4660b93`، وعُدِّل المحلّل ليُبقي تبعيات الجذع (المرجع) في بقية الموجات |
| `claude/q19-workout-layout-order` | اتحاد `test:workout-order` |
| `claude/q20-exercise-media-quality` | اتحاد `media:audit`/`test:exercise-media` |

### الموجة 4 — تعارضات كود حقيقية (7 مدموجة · 5 موقوفة)

**مدموجة:**

| # | الفرع | الحل |
|---|---|---|
| 20 | `origin/claude/h1-session-resume` | اتحاد قاموس `workoutScreen.ts`: أُضيفت مفاتيح `resume*`، وبقي `switchHere: 'بدّل له'` (الأحدث، موجة اللهجة 2026-07-25) بدل `'بدّل إليه'` (WIP قديم) |
| 22 | `origin/claude/account-creation-bracket-error-rzxfv0` | اتحاد استيراد `App.tsx` (`adoptGuestOnboarding` + `reconcileAccountScope`). §4.1: `loadOnboarding` صار ميتاً (الجذع تجاوز استخداماته) فأُزيل؛ الميزة الفعلية `adoptGuestOnboarding` محفوظة |
| 25 | `e/calc-adopt` | ملفات الحاسبة **متطابقة مع الجذع أصلاً**؛ تعارض ProgressV2 تنسيق بحت، حُسم لصالح الجذع (الهوية الكلاسيكية `450f497` الأحدث). الجذع يحوي كل روابط الحاسبة ووظائفها |
| 26 | `e/steps-adopt` | ملفات الخطوات **متطابقة مع الجذع**؛ ProgressV2 حُسم لصالح الجذع (زر «خطواتك» وظيفي كامل بالجذع) |
| 29 | `fix/signup-numerals-dialect` | `passwordPolicy.ts` **متطابق مع الجذع** (الإصلاح موجود)؛ proof الجذع superset (18 فحص) |
| 30 | `site/brand-email-update` | علامة العلامة **متطابقة مع الجذع أصلاً**؛ 3 صفحات HTML حُسمت لصالح الجذع (تاريخ حقيقي بدل placeholder `[DATE]`، أقسام خصوصية أحدث). بريد الدعم موحّد أصلاً |
| 31 | `import/codex-p0-batch1` | حزمة P0 (35 ملف). `NutritionV2`: **اتحاد** — ميزة «وجبات محفوظة» (الفرع) + تنسيق تنويه بأيقونة (الجذع). `e2e-onboarding.mjs`: أُخذت نسخة الفرع لأن عقده المُدمج في البوابة (`test:onboarding-e2e-contract`) مقترن بها؛ `package.json` اتحاد |

**موقوفة (قرار مؤسس — منطق متعارض/متجاوَز):**

| # | الفرع | السبب |
|---|---|---|
| 21 | `h/workout-session-resume` | **آلية استئناف منافسة** للجذع: الجذع يملك `reconcileWorkoutColdStart` + `RecoveryDecisionSheet`؛ الفرع `resumeOffer` + `ResumeSheet`. منطق متعارض لا اتحاد أسطر → اختيار الآلية قرار منتج |
| 23 | `e/settings-clarity-r2` | **متجاوَز بنيوياً:** الجذع `eac43a9` (2026-08-02، أحدث من الفرع) حوّل SettingsView إلى «تبويبات الإصدار القديم». الفرع يعيد التنظيم بلوحات جديدة ويُسقط تصدير البيانات — اتجاهان متنافيان |
| 24 | `claude/q18-health-discoverability` | مبني على **نفس بنية إعادة التنظيم** (يستورد `NotificationSettingsPanel`/`NativeSettingsPanel`) التي تجاوزها `eac43a9`؛ جوهره متشابك معها. أجزاؤه النظيفة (route + `HealthLinkView`) لا تُدمج جزئياً في git |
| 27 | `claude/q16-today-greeting-contrast` | **متجاوَز:** الجذع `eac43a9` أزال مفهوم البطاقة المميّزة (`featured`: 0 مرجع مقابل 11 في الفرع). دمجه يعكس أحدث تصميم، وسكربت إثباته يكسر البوابة. **ملاحظة للمؤسس:** نموذج `todayV2Model` يحسب `dailyPhrase` لكن نسخة الجذع من الشاشة لا تعرضها — تحقّق إن كان مقصوداً |
| 28 | `claude/q21-muscle-map-replacement` | **قرار §11 المجمّد:** يستبدل `BodyModel3D` (الساكن المؤقت الحالي في الجذع) بـ`MuscleBodyMap` — وهذا «قلب الساكن» المحجوز لدراسة خريطة العضلات المجدولة للمؤسس |

---

## 4. الموجة 5 — البنية التحتية (2 مدموجة · 2 موقوفة)

**مدموجة:**

| # | الفرع | الحل |
|---|---|---|
| 32 | `claude/p14-supabase-schema-rls` | doc: بقيت ملاحظة الجذع «التحقق الحي لم يُنفَّذ» (أحدث)؛ package.json اتحاد. **ملاحظة:** يُدمج ملفات الهجرات إلى فرع التكامل فقط — **تطبيقها على قاعدة حيّة إجراء نشر مُبوَّب منفصل** (`test:db-schema` أخضر، `db:verify` يبقى للتحقق الحي) |
| 33 | `claude/p14-ios-native-hardening` | doc `DEVICE-NOT-VERIFIED.md`: نسخة الجذع أحدث وأدقّ (لا أقسام فريدة في الفرع)؛ package.json اتحاد؛ `test:native-hardening` 93/0 |

**موقوفة:**

| # | الفرع | السبب |
|---|---|---|
| 34 | `claude/p14-e2e-release-gate` | **تعارض معماري e2e مع الفرع 31 (المدموج):** نسخته من `e2e-onboarding.mjs` لا تحوي أنماط عقد الفرع 31، ولا يحدّث ذلك العقد — فدمجه يكسر `test:onboarding-e2e-contract` في البوابة، وإزالته تعطيلُ اختبار (ممنوع §4). اختيار معمارية e2e (31 مقابل 34) قرار للمؤسس/مالك الاختبارات |
| 35 | `origin/content/food-r2-gcc-eatingout` | **متجاوَز:** الجذع يملك الـ60 صنف R2 خليجي كاملة ومُثبَتة عبر `foodR2EatingOut` (البروف يفحص `r.r2 === 60`). الفرع نسخة مكرّرة (`gccEatingOut`) دمجها يضيف ملفاً ميتاً غير مستورد |

---

## 5. الموجة 6 — الأعمال المُنقَذة (1 مدموج)

**مدموج:**

| # | الفرع | الحل |
|---|---|---|
| 41 | `rescue/ux-entry-onboarding-polish` | **تحسينات وصولية حقيقية منقَذة** (`aria-live`، `role="alert"`، تباين AA، إخفاء الأيقونات الزخرفية) لـLoginView/ResetPasswordView — جديدة على الجذع ومتوافقة مع §9. تعارض عدّاد خطوات OnboardingV2 حُسم لصالح الجذع الأحدث؛ build + إثباتات onboarding خضراء |

**الاستبعادات الموقّعة في [CTO-61] (لم تُلمس):**

- **#37 `rescue/codex-qimmah-rc-integration`** — مستبعد (المسار الموصى: الفروع المنفردة Q15–Q21، وهي في الموجات 3–4). **يبقى أرشيفاً مرجعياً — لم يُحذف.**
- **#40 `rescue/legal-data-region-japan`** — مستبعد، معلّق بانتظار فحص قانوني منفصل من المؤسس. لم يُدمج منه أي ملف.
- **`fix/audit-cleanup-wave`** — مستبعد نهائياً (48 ملف تعارض). محفوظ كما هو → مؤجَّل للتنقيب اليدوي.

**مؤجَّلة/مرفوضة (بقية المُنقَذة) — محفوظة كسجلّ، لم تُحذف:**

| الفرع | الحكم |
|---|---|
| #36 `rescue/codex-structure-uiux-v1` ★★ | **مؤجَّل لمراجعة المؤسس اليدوية** — 15 تعارضاً، قيمة عالية/مخاطرة عالية (إعادة بناء بنية جوّالة، 6 إيداعات لم تُدفع قط). الأودِت أوصى «راجعه يدوياً أولاً» — لا يُدمَج آلياً |
| #39 `rescue/p12-gif-assets` | **مرفوض قطعاً** — يعيد إدخال 92 أصل GIF من WorkoutX **تحمل علامة مائية لمصدر مملوك**، حذفها الجذع في إصلاح P0 قانوني (خريطة `exerciseGifs` مفرَّغة عمداً). دمجه ينقض إصلاحاً قانونياً؛ `test:media-rights` يحرسه |
| #38 `rescue/integration-phase25-merge` | **تخطّي — no-op:** `merge-tree` مقابل الرأس = صفر تغيير صافٍ (محتواه في الجذع أصلاً/متجاوَز، حقبة 2026-06-30) |
| #42 `rescue/p25-saudi-restaurant-foods` | **تخطّي:** بيانات `foodItems` R2 موجودة في الجذع أصلاً (انظر #35)؛ الأودِت أوصى «استخرج البيانات فقط» — عملية يدوية لا دمج فرع |
| #43 `rescue/mobile-qa-routes-polish` · `rescue/workout-history-binding-wip` | سجلّ فقط (حقبة يونيو، ما قبل التوحيد) — الأودِت: «لا يُنصح بالدمج» |
| #44 `rescue/recovery-v11-merge{,-alt}` · `q20-media-perf-baseline` · `audit-cleanup-wave-wip` · `saudi-foods-nutritionv2-wip` · `ux-core-mobileshell-wip` · `p12-*-wip` · `stash-*` | سجلّ فقط (متجاوَزة أو مجهرية) |

---

## 6. نمط متكرّر اكتُشف أثناء التنفيذ

عدة فروع (خاصة حارة التصميم E وفروع `claude/q*`) تبيّن أنها **متجاوَزة بتحوّلات الجذع الأحدث** التي هبطت 2026-08-02 (`eac43a9` «تبويبات الإصدار القديم»، `450f497` «الهوية الكلاسيكية»). الأودِت قِيس قبلها، فبعض «المرشحين» صاروا متجاوَزين لحظة التنفيذ — تطبيقٌ حيّ لمبدأ §2 «القرارات تشيخ كما تشيخ الفروع». في كل حالة: قُيس الفرق فعلياً (تواريخ الإيداعات، تطابق المحتوى، مراجع الرموز) قبل الحكم، ووُثّق الخياران بدل الحسم الاجتهادي.

---

## 7. الامتثال للقواعد الحاكمة (كما في [CTO-61])

| القاعدة | الحالة | الدليل |
|---|---|---|
| لا دمج في `main` | ✅ | `main` = `origin/main` = `658d088` دون تغيير |
| لا `push` إلى `origin/main` | ✅ | لم يُنفَّذ |
| لا إعادة كتابة تاريخ | ✅ | لا `rebase`/`reset --hard`/`filter-branch` |
| لا حذف فرع | ✅ | صفر حذف؛ كل الموقوفة سليمة |
| لا حذف stash | ✅ | كلتا الـstashين سليمتان (المحميّة `stash@{1}` + stash النرد `stash@{0}`) |
| `package.json` باتحاد التبعيات لا اختيار طرف | ✅ | §3، مع انحراف موثّق وحيد صُحِّح (`react-body-highlighter`) |
| توقّف عند الالتباس الدلالي + توثيق الخيارين | ✅ | 7 فروع أُوقفت بتوثيق كامل |
| بوابة كل موجة قبل التالية | ✅ | 6/6 خضراء + وسوم |

---

## 8. الخطوة التالية — [CTO-62]

- فرع التكامل `integration/nard-2026-08` (`9638adf`) جاهز للدفع إلى `origin` **كنسخة احتياطية بعيدة** (إجراء إضافي مسموح؛ `origin/main` لا يُلمس).
- **الدمج النهائي في `main` مُبوَّب بـ[CTO-62]** بعد اجتياز رحلات e2e الحرجة على فرع التكامل. لم يُنفَّذ في هذه الجلسة.
- **قرارات المؤسس المطلوبة قبل [CTO-62]:** الفروع الموقّفة السبعة (21، 23، 24، 27، 28، 34) + #36 (مراجعة يدوية) + #40 (فحص قانوني) + تطبيق هجرات #32 على قاعدة حيّة.

---

*انتهى التقرير.*

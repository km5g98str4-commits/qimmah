# QIMMAH-QUALITY-CHARTER.md — دستور الجودة

> **مرجع كل بوابة قادمة.** كل موجة، كل PR، كل هبوط يُقاس بهذا الملف.
> صدر بموجب **[CTO-64]** · الأساس: `main` @ `7c726b9` · 2026-08-02
> **علاقته بالميثاق:** `AGENTS.md`/`CLAUDE.md` يحكمان **كيف نعمل** (الفروع، الصلاحيات، الحارات). هذا الملف يحكم **متى نقول إن العمل انتهى**. عند أي تعارض: **الميثاق يفوز في الإجراء، وهذا الملف يفوز في تعريف الاكتمال.**

---

## 1. تعريف الاكتمال — البنود السبعة

> **الميزة ليست «مكتوبة». الميزة «تصل المستخدم وتعمل».**
> لا تُعلَن ميزة مكتملة قبل اجتياز **البنود السبعة كاملة**. سبعة من سبعة أو غير مكتملة — لا نسب مئوية.

### ١-١ تعمل محليًا من طرف المستخدم

يُشغَّل التطبيق ويُسلَك المسار **كما يسلكه المستخدم**، من نقطة دخوله الحقيقية إلى نتيجته المحفوظة.

- **لا يكفي أن يبني الملف.** الاستيراد الساكن ليس دليل حياة.
- **افحص الوصولية صراحةً:** هل هناك نداء تنقّل حيّ إلى هذه الشاشة من ملف **غير يتيم**؟
- **افحص الحزمة:** `npm run build` ثم تأكّد أن للميزة أثرًا في `dist/assets/`. **ملف لا يظهر في الحزمة لا يصل المستخدم مهما كان سليمًا.**

> **الواقعة المؤسِّسة (جرد [CTO-64]):** ثلاث شاشات V2 بمجموع ٢٥١٦ سطرًا — تحمل عمل Q19 وQ20 وQ21 وحزمة P0 — كانت **يتيمة وغائبة عن الحزمة كليًا**، وثلاثة مسارات مسجّلة كانت غير قابلة للوصول. كل البوابات كانت خضراء. **البوابة الخضراء لم تكن كاذبة — كانت تقيس الطبقة الخطأ.**

**البند العملي:** كل موجة تُنهي تقريرها بسطر **«مسار الوصول»**: من أين يبدأ المستخدم، وبكم نقرة يصل الميزة. **لا سطر ⇒ لا اكتمال.**

### ١-٢ البوابات الأربع خضراء

```bash
npm ci && npm run typecheck && npm run lint && npm run build && npm run test:gate
```

- `npm ci` **لا** `npm install` — القفل حتمي ويحذف الزائد.
- تُرفق النتائج الفعلية بأرقامها في التقرير، لا ادّعاء «خضراء».
- **وقراءة CI الأخير قانون لا عادة** (§4.0 من الميثاق) — البوابة المحلّية لا تشمل خطوات المتصفح.

### ١-٣ لا أسرار في المستودع

- لا مفاتيح ولا توكنات ولا كلمات سر في أي ملف مُتتبَّع.
- **كل `VITE_*` مرئي للعموم في حزمة الإنتاج** — لا يُستخدم إلا لقيم عامة.
- `.env` في `.gitignore`؛ `.env.example` بقيم وهمية.

### ١-٤ لا بيانات وهمية في مسار إنتاجي

- كل بيانات البذر أو النموذج **محجوزة خلف `import.meta.env.DEV`** أو علم صريح.
- **حين لا نعرف الرقم، نعرض «—» لا رقمًا مخترعًا.** (النمط المعتمد: `TodayV2:MacroStat` — بلا هدف مضبوط لا رقم.)
- لا `Lorem ipsum` ولا نص مؤقت.

### ١-٥ لا أزرار ولا مداخل ميتة

- كل زر يفعل شيئًا مرئيًا: تنقّل · تغيير حالة · رسالة.
- **وكل شاشة لها مدخل حيّ.** شاشة مسجَّلة في المسارات بلا نداء تنقّل إليها = زر ميت بحجم شاشة.
- **وكل مسار له وجهة.** `href` إلى مرساة لا وجود لها رابط مكسور.
- عنصر غير قابل للنقر **لا يلبس هيئة القابل للنقر**. إن لم يكن `button` فلا يأخذ شكل البطاقة القابلة للنقر.

### ١-٦ حالات التحميل والفراغ موجودة

- **تحميل:** هيكل (`skeleton`) لا شاشة بيضاء ولا قفزة تخطيط.
- **فراغ:** نصّ يشرح ما الذي سيملأ المكان **وما الخطوة التالية** — لا فراغ صامت.
- **خطأ:** حدّ أخطاء يعرض مخرجًا فعّالًا. (النمط المعتمد: `ErrorBoundary` مع «أعد المحاولة» التي **تُعيد الاستيراد فعلًا** لا تعيد الرسم فقط.)

### ١-٧ الأخطاء مفهومة للمستخدم

- رسالة الخطأ تقول **ما حدث** و**ما العمل** — بالعامية البيضاء، بلا مصطلح تقني ولا رمز خام.
- **الرسالة تسمّي السبب الحقيقي.** رسالة عامة عن سبب معروف = خطأ جودة، لا نقص تحسين.
- لا لوم ولا نبرة اتهام. المستخدم لم يخطئ — النظام لم يقبل.

---

## 2. الممنوعات الدائمة

> **هذه ليست تفضيلات. مخالفتها تُسقط الموجة مهما كان ما فيها.**

### ٢-١ ممنوع تعطيل اختبار ليمرّ بناء

حذف اختبار · تعليقه · تخفيف تأكيده · إخراجه من `test:gate` — **كله ممنوع** ما لم يكن **استبعادًا معلَنًا** بسطر في رأس السكربت يشرح السبب وما يغطّيه بديلًا، ومذكورًا في التقرير.

> **التعطيل الممنوع هو الصامت.** المعلَن مسموح ومحسوب.

### ٢-٢ ممنوع `any` عشوائي

- TypeScript `strict`. كل `any` يحتاج مبرّرًا مكتوبًا في سطر فوقه.
- `as unknown as X` لتجاوز فحص النوع ممنوع في كود جديد.
- المدخل غير الموثوق (استيراد · مزامنة · شبكة) يُعبَر عنه `unknown` **ويمرّ بطبقة تحقّق**، لا `any`.

### ٢-٣ ممنوع ابتلاع الأخطاء بصمت

- `catch {}` فارغ ممنوع.
- **فشل الحفظ لا يُبتلع أبدًا:** لا شاشة نجاح ولا مسح حالة قبل تأكيد الكتابة.
  **النمط المعتمد** (تدفّق إنهاء التمرين): تأكيد ← كتابة ← فحص `WriteResult` ← عند الفشل: استرجاع اللقطة + رسالة صادقة + **بقاء البيانات**.
- كل كتابة تخزين تمرّ عبر `safeStorage` مع فحص النتيجة — لا `localStorage` خام.

### ٢-٤ ممنوع `TODO` في مسار أساسي

مسار أساسي = ما يمرّ به المستخدم ليصل قيمة التطبيق: الدخول · الإعداد · اليوم · التمرين · التغذية · التقدّم · الحساب.

- `TODO` أو `FIXME` أو «قريبًا» أو «لاحقًا» في هذه المسارات = **الميزة غير مكتملة**، تُصنَّف [مكتملة جزئيًا] ولا تُعلَن جاهزة.
- الأفكار الجانبية → `docs/product/BACKLOG.md` لا تعليق في الكود.

### ٢-٥ ممنوع الحلول المؤقتة لتمرير البوابة

- لا تعديل بيانات اختبار لتوافق كودًا معطوبًا.
- **ومرور غير مستحقّ ليس نجاحًا:** إن أمكن إرضاء تأكيد بنقض مقصده المُعلَن، فالبوابة رخوة — **بلّغ ولا تستغلّ، ثم شُدّ البنية**.
- كل شدّ بوابة يُرفَق بمحاكاة التفافٍ تفشل بفحص مسمّى.

### ٢-٦ ممنوع الكود اليتيم بلا إعلان

ملف لا يستورده أحد **يُعلَن في تقرير الموجة** إمّا للحذف وإمّا بسبب بقائه.

> **سبب القاعدة:** ٢٧ ملفًا يتيمًا (~٤٩٠٠ سطر) تراكمت بلا إعلان، فبقيت شاشات كاملة تبدو مبنيّة وهي لا تصل أحدًا.

**وفحص الاستيراد الساكن ليس شهادة وفاة:** قبل حذف أي مرشّح، `grep` للمراجع الديناميكية (مفاتيح نصية · `lazy` imports · جداول مسارات) موثّقًا في التقرير.

---

## 3. القرارات المؤسسية المقفلة

> **لا تُعاد فتحها ولا تُناقَش في موجة.** تغييرها يحتاج رسالة `[CTO-n]` صريحة من المؤسس.

### ٣-١ حدّ العمر: ١٣ سنة

- قِمّة لعمر **١٣ فما فوق** — رقم واحد موحّد في كل موضع نصّي وكل فحص.
- من دون ١٣ **يُمنع من الأهداف المقيَّدة**، ويرى **رسالة تسمّي السبب بهدوء وبالعامية البيضاء** — لا رسالة حقول عامة ولا نبرة اتهام.
- حدّ دفاعي عند **مدخل توليد الخطة** لا عند الواجهة وحدها.
- **بند امتثال App Store** — يسبق أي رفع لـTestFlight.

### ٣-٢ سجلّان للنبرة: عامية بيضاء + فصحى للقانوني

| السجلّ | أين |
|---|---|
| **عامية بيضاء** (سعودية/خليجية دافئة يفهمها كل عربي) | الواجهة · التسويق · الدعم |
| **فصحى** | **القانوني حصرًا** — الخصوصية والشروط |

- **شاشة واحدة = سجلّ واحد.** لا خلط.
- **الكتلة القانونية صوت ثانٍ معلَن مفصول بصريًا** — تُعلن نفسها ولا تندسّ في نبرة الشاشة.
- الفصحى في واجهة المستخدم **دَين** يُسدَّد تدريجيًا: كل موجة تحوّل ما تمسّه.
- الإنجليزية المرافقة غير رسمية ودودة.
- **الثوابت الأربعة:** لا لوم/ضغط/تهويل · اللغة المتحفّظة للمُستنتَج («تقريبي» · «يبدو أنك») والحاسمة للمُقاس · شاشة = سجلّ · **الصدق قبل الطمأنينة**.

### ٣-٣ تعديل الخطة اقتراح بسبب — لا تعديل آلي صامت

- في v1، أي تغيير على خطة المستخدم **يُعرَض اقتراحًا** ويُشرح **سببه** مربوطًا بمدخل المستخدم.
- **لا تعديل آلي صامت مهما كان مبرّرًا.** الخطة ملك المستخدم.

### ٣-٤ موافقة ثانية منفصلة للبيانات الصحية

- بيانات الصحة الحسّاسة **تُزامَن خلف موافقة صريحة منفصلة** عن موافقة المزامنة العامة.
- موافقة واحدة لا تغطّي الاثنتين. المنقّي (`syncFieldPolicy`) يحرس الحدّ.

### ٣-٥ المزامنة مطفأة حتى إشعار المؤسس

- `VITE_SYNC_ENABLED` افتراضه **OFF**، ولا يُفعَّل إلا بإشعار مؤسس صريح.
- التفعيل مشروط بـ: تحقّق مخطّط Supabase وسياسات RLS · حسم **تعارض المزامنة الكامن** الموثّق (`onboardingProfile` يُدرِج الملف الكامل بينما `onboardingSync` يكتب لقطة مصغّرة) · وموافقتَي ٣-٤.
- **الفلسفة المعلنة:** محلي افتراضيًا، مزامنة سحابية اختيارية عند تسجيل الدخول. **أي نص أو كود يخالفها يُصحَّح.**

### ٣-٦ قرارات مقفلة موروثة من §8 من الميثاق

- الميزانية الغذائية **خارج v1**.
- سؤال رمضان **دائم بصياغة محايدة**، يُفعَّل موسميًا.
- مصدر مكتبة التمارين الموسّعة **معلّق** حتى اكتمال جرد المكتبة الحالية.
- **ترقية الإصدار (`version`) فعل إطلاق محجوز للمؤسس.**

---

## 4. الوصولية — حدّ أدنى غير قابل للتفاوض

- **تباين AA:** نصّ عادي ≥ **4.5:1** · نصّ كبير (≥ 18.66px عريض أو ≥ 24px) ≥ **3:1** — **في الثيمين الفاتح والداكن معًا**.
- **لا لون وحده حاملًا للمعنى** — يرافقه أيقونة أو نصّ أو حدّ.
- **هدف اللمس ≥ 44×44 بكسل.**
- تسمية لكل حقل · إدارة تركيز في كل نافذة · `aria-label` لكل زر أيقوني.
- **تسلسل العناوين سليم** (`h1` → `h2` → `h3` بلا قفز).
- **RTL أولًا:** خصائص منطقية حصرًا (`ms-` `me-` `ps-` `pe-` `text-start` `text-end` `start-` `end-`) — لا `left/right`. يُفحص بالعربية والإنجليزية معًا.
- **الحالة المحجوبة تُرى:** `aria-disabled` وحده لا يكفي — يرافقه فرق بصري واضح.

---

## 5. الصدق — المبدأ الحاكم

> **الصدق قبل الطمأنينة. حين لا نعرف، نقولها صريحة ونعطي الخطوة التالية.**

- لا ادّعاءات طبية ولا وعود نتائج.
- **لا تُعتبر الواجهة دليلًا على عمل الميزة** — يُفحص المسار كاملًا من الزر إلى التخزين.
- **لا تُدّعى ميزة مكتملة قبل اختبارها فعليًا**، ولا يُكتفى بتقرير وكيل فرعي.
- المكمّلات والأدوية للعرض والتتبّع فقط، مع تنويه عند الحاجة.
- **ما لا نعرفه يُعرض «—»**، وما نستنتجه يُقال بلغة متحفّظة، وما نقيسه يُقال حاسمًا.

---

## 6. بطاقة البوابة — تُملأ في كل تقرير موجة

```
□ ١-١  مسار الوصول: يبدأ من ______ → ______ (___ نقرات). فحص الحزمة: ______
□ ١-٢  npm ci · typecheck · lint · build · test:gate  → النتائج: ______
□ ١-٣  لا أسرار جديدة (ولا VITE_* حسّاس)
□ ١-٤  لا بيانات وهمية خارج DEV
□ ١-٥  كل زر يفعل شيئًا · كل شاشة لها مدخل حيّ · لا رابط مكسور
□ ١-٦  حالات التحميل والفراغ والخطأ موجودة
□ ١-٧  رسائل الخطأ تسمّي السبب وتعطي الخطوة التالية بالعامية البيضاء
□ ٢-*  لا اختبار معطَّل صامتًا · لا any عشوائي · لا catch فارغ · لا TODO في مسار أساسي
□ ٢-٦  الملفات اليتيمة المكتشفة: ______ (للحذف / سبب البقاء)
□ ٣-*  لا مساس بقرار مقفل — وإن لزم: رُفع ولم يُحسم اجتهادًا
□ ٤    تباين AA في الثيمين · لمس ≥44px · تسلسل عناوين · RTL بالخصائص المنطقية
```

**بطاقة ناقصة = موجة غير مكتملة.**

---

## 7. ما يُرفَع ولا يُحسَم اجتهادًا

- تعارض بين المعيار التصميمي والكود القائم.
- تعارض بين مادة غرفة محتوى والميثاق أو قرار مقفل — **لا إصلاح صامت**: يُدرَج ما صحّ وتُرفَع المخالفة بندًا مسمّىً.
- **وثيقة أو قاعدة بائتة.** القرارات تشيخ كما تشيخ الفروع.
  > **قائم الآن:** `AGENTS.md`/`CLAUDE.md` §1 يعلنان الجذع `design/v21-promotion` و«`main` لا تُمس» — بائت بعد [CTO-63]. و`.claude/rules/product.md` يصف المشروع «قالبًا تجاريًا للبيع» و«Frontend فقط بلا مصادقة» — يناقض `CLAUDE.md` §0 وواقع الكود. **كلاهما مرفوع للتحديث.**
- أي فعل لا رجعة فيه: ترقية `main` · نشر · رفع للمتجر · حذف فرع · تطبيق هجرة على قاعدة حيّة.

---

*هذا الملف مرجع كل بوابة قادمة. تعديله يحتاج رسالة `[CTO-n]`.*

# سجلّ التبعيات على HEAD النهائي — برنامج ما بعد الويب

**القاعدة (أمر المؤسس 2026-08-14):** «اعتبر Web Sovereign تبعية واحدة فقط. لا تنتظرها إلا عند النقطة
التي تحتاج HEAD النهائي فعلًا. إذا وجدت بندًا يعتمد على HEAD النهائي: سجّله Dependency، وانتقل مباشرة للبند التالي.»

**الهدف:** أن تكون لحظة فتح البوابة **دمجًا وربطًا**، لا بداية مشروع.

## حالة التبعية نفسها

| البند | القيمة |
|---|---|
| الفرع | `codex/qimmah-web-sovereign-001` |
| آخر checkpoint مرصود | `1bcf7a9` = `[PKG-7][green]` (2026-08-14 14:08) |
| أساس كل فروع هذا البرنامج | `1bcf7a9` |
| ما تبقّى في مشواره | Layer 3 (Profile) · Layer 4 (تدقيق أهداف اللمس لكل الموقع) · Layer 6 (الأداء) |
| المراقب | فعّال — ينبّه عند كل checkpoint، وعند التعثّر، وعند فتح البوابة |

> **الأساس متحرّك عمدًا.** فروعنا مبنيّة على `1bcf7a9`، والمشوار يتقدّم فوقه. وهذا مقبول
> لأن جلّ عملنا **ملفات جديدة** لا تلامس ما يعدّله — فالدمج الأمامي يكاد يكون بلا تعارض.
> ما يلامس ملفاته يُسجَّل هنا ولا يُنفَّذ.

## سجلّ التبعيات — يملؤه كل حارة في فرعها، ويُوحَّد هنا

| # | البند | لماذا يحتاج HEAD النهائي | الفعل عند الهبوط | المصدر |
|---|---|---|---|---|
| D-01 | ترحيل `src/features/products/store.ts` من `localStorage` الخام إلى تسليم مُشظّى + `IndexedDB` | ملف حيّ في سطح التغذية الذي يعدّله المشوار | تطبيق العقد في `SEARCH-INDEX-ARCHITECTURE.md` §5 | المنسّق |
| D-02 | إصلاح تجاوز `safeStorage` في نفس الملف (خرق ميثاق §5 قائم) | نفس الملف أعلاه | يُدمج مع D-01 في موجة واحدة | المنسّق |
| D-03 | تحويل `src/data/foodItems.ts` من مصفوفة مضمّنة إلى «الطبقة الساخنة» | مستهلَك من واجهة التغذية الحيّة | استبدال المصدر مع إبقاء الواجهة | المنسّق |
| D-04 | اعتماد `src/lib/exerciseLibrary.ts:22` للمطبِّع العربي المشترك | `PKG-6` يملك سطح المكتبة الآن | سطر واحد: استبدال `includes` بالمطبِّع + الفهرس | المنسّق |
| D-05 | ربط واجهة البحث بالفهرس بدل `filter` الخطّي | واجهة حيّة | بعد D-01 | المنسّق |

*(الحارات تضيف بنودها في `docs/execution/qimmah-postweb/<حارة>/DEPENDENCIES.md`؛ المنسّق يوحّدها هنا عند كل تقرير.)*

## ما ليس تبعية — ويُنفَّذ الآن

خطوط الاستيراد · المخطّطات · العقود · الـmanifests · الـfixtures · جداول الربط · تعاريف التحليلات ·
مواصفات البحث · حصاد الاختبارات · الـworktrees المستقلة. **كلها ملفات جديدة لا تتقاطع مع سطوح المشوار.**

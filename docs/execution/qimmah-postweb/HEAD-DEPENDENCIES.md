# سجلّ التبعيات على HEAD النهائي — برنامج ما بعد الويب

**القاعدة (أمر المؤسس 2026-08-14):** «اعتبر Web Sovereign تبعية واحدة فقط. لا تنتظرها إلا عند النقطة
التي تحتاج HEAD النهائي فعلًا. إذا وجدت بندًا يعتمد على HEAD النهائي: سجّله Dependency، وانتقل مباشرة للبند التالي.»

**الهدف:** أن تكون لحظة فتح البوابة **دمجًا وربطًا**، لا بداية مشروع.

## حالة التبعية نفسها

| البند | القيمة |
|---|---|
| الفرع | `codex/qimmah-web-sovereign-001` |
| آخر checkpoint مرصود | `1bcf7a9` = `[PKG-7][green]` (2026-08-14 14:08) |
| أساس كل فروع هذا البرنامج | `1bcf7a9` |
| ما تبقّى في مشواره | Layer 3 (Profile) · Layer 4 (تدقيق أهداف اللمس لكل الموقع) · Layer 6 (الأداء) |
| المراقب | فعّال — ينبّه عند كل checkpoint، وعند التعثّر، وعند فتح البوابة |

> **الأساس متحرّك عمدًا.** فروعنا مبنيّة على `1bcf7a9`، والمشوار يتقدّم فوقه. وهذا مقبول
> لأن جلّ عملنا **ملفات جديدة** لا تلامس ما يعدّله — فالدمج الأمامي يكاد يكون بلا تعارض.
> ما يلامس ملفاته يُسجَّل هنا ولا يُنفَّذ.

## سجلّ التبعيات — يملؤه كل حارة في فرعها، ويُوحَّد هنا

| # | البند | لماذا يحتاج HEAD النهائي | الفعل عند الهبوط | المصدر |
|---|---|---|---|---|
| D-01 | ترحيل `src/features/products/store.ts` من `localStorage` الخام إلى تسليم مُشظّى + `IndexedDB` | ملف حيّ في سطح التغذية الذي يعدّله المشوار | تطبيق العقد في `SEARCH-INDEX-ARCHITECTURE.md` §5 | المنسّق |
| D-02 | إصلاح تجاوز `safeStorage` في نفس الملف (خرق ميثاق §5 قائم) | نفس الملف أعلاه | يُدمج مع D-01 في موجة واحدة | المنسّق |
| D-03 | تحويل `src/data/foodItems.ts` من مصفوفة مضمّنة إلى «الطبقة الساخنة» | مستهلَك من واجهة التغذية الحيّة | استبدال المصدر مع إبقاء الواجهة | المنسّق |
| D-04 | اعتماد `src/lib/exerciseLibrary.ts:22` للمطبِّع العربي المشترك | `PKG-6` يملك سطح المكتبة الآن | سطر واحد: استبدال `includes` بالمطبِّع + الفهرس | المنسّق |
| D-05 | ربط واجهة البحث بالفهرس بدل `filter` الخطّي | واجهة حيّة | بعد D-01 | المنسّق |

## ما وحّدته من الحارات الثلاث الهابطة

### حارة التمارين — `codex/qimmah-exercise-production-001`

| # | البند | الفعل عند الهبوط |
|---|---|---|
| C-1 | وصل نافذة التفصيل بالمانيفست الموحّد | `getCue(ex.id)` → `getCue(ex.id, lang)`؛ ومصدر الفيديو من المانيفست لا `ex.videoUrl` |
| C-2 | تسريب بطاقة الجهاز في شبكة المكتبة | استدعاء `isPlaceholderOnlyMedia` في محلّل صورة البطاقة كما في `ExerciseMedia.tsx:122` |
| C-3 | `getVideoLabel` كود ميت بمفردات لا تطابق الواقع | يُحذف أو يُعاد ربطه بحالات المانيفست |
| C-4 | **قرار مؤسس معلَّق** — نبرة عنوانَي ATHLEAN-X | استعادة أو إبقاء `NEEDS_REVIEW` |
| C-5 | `test:guidance-honesty` — وصف فحص يصير غير مطابق بعد C-1 | إعادة عنونة الفحص بنصّه الجديد |

### حارة الطعام — `codex/qimmah-food-production-001`

| # | البند | الفعل عند الهبوط |
|---|---|---|
| B-1 | ترحيل مخزن المنتجات إلى التسليم المُقطَّع + `IndexedDB` | يشمل إصلاح `localStorage` الخام في `store.ts:28/38/47/57` (خرق §5) وقتل المسح الكامل في `searchProducts` |
| B-2 | **نسب المصدر ODbL في الواجهة — التزام رخصة لا تحسينًا** | إضافة سطر النسب بالعربية والإنجليزية في سطح التغذية |
| B-3 | تبنّي المُطبِّع المرجعي في مكتبة التمارين | `exerciseLibrary.ts:22` يستدعي `@/lib/text/foodNormalize` |

> **B-2 بند امتثال، لا تجميل.** بيانات OpenFoodFacts تحت رخصة ODbL، والنسب شرطها.
> شحن البيانات بلا نسب مخالفة ترخيص — تُعالَج قبل أي إطلاق يشحن هذه القاعدة.

**وحارة الطعام نفّذت مواصفة البحث (§٣–§٥) وقاست كلفتها بدل ادّعائها:**
الطيّ الثماني ✅ · NFD ✅ · «ال» إضافة لا استبدال ✅ · وحدة نقيّة `NORMALIZATION_VERSION 1.1.0` ✅ ·
بادئات ٣–٨ ✅ بكلفة **+٦٨٪ للفهرس ≈ +٦٫٥٪ للإجمالي** · الباركود مسار مفتاح `O(1)` ✅ · الطبقتان الساخنة والذيل ✅.

### حارة اللوحة — `codex/qimmah-executive-dashboard-001`

| # | البند | الفعل عند الهبوط |
|---|---|---|
| E-1 | **تصريح الدور أولًا** — لا شيء يُعرض قبله | قرار مؤسس: مطالبة JWT أم جدول `admin_users` |
| E-2 | الوصل في القشرة — ثلاثة تعديلات لا أكثر | `appRoutes.ts` (+`admin`، **لا** في `MAIN_TABS`) · `App.tsx` فرع خارج `MobileShell` · مضيف حالة صغير |
| E-3 | نقاط النهاية التسع الناقصة | دالّة `security definer` على `profiles` تفتح ٤ مقاييس فورًا |

**وما لا يُفعل عند الوصل:** لا مدخل مرئي للمستخدم العادي · لا توسيع لنطاق المزامنة ولا مسّ للموافقات
لإحضار أرقام نشاط · ولا أفعال مدمّرة بلا قدرة خادم مُراجَعة.

---

*(الحارات تضيف بنودها في `docs/execution/qimmah-postweb/<حارة>/DEPENDENCIES.md`؛ المنسّق يوحّدها هنا عند كل تقرير.)*

## ما ليس تبعية — ويُنفَّذ الآن

خطوط الاستيراد · المخطّطات · العقود · الـmanifests · الـfixtures · جداول الربط · تعاريف التحليلات ·
مواصفات البحث · حصاد الاختبارات · الـworktrees المستقلة. **كلها ملفات جديدة لا تتقاطع مع سطوح المشوار.**

# إثبات — موجة الصقل ٢ (الوعود/الوصول)

الأساس: `53158f7`. iPhone 15 · 393×852pt @2x — عبر `scripts/polish2-shot.mjs`.

| اللقطة | ما تثبته |
|---|---|
| `20-return-honest.png` | هيرو العودة صادق: «عُد بلطف اليوم · ابدأ بأول ١٥ دقيقة من خطتك وأكمل إن أحببت · تقدّمك محفوظ» + CTA «ابدأ تمرين اليوم» + بطاقة واحدة «راجع تقدّمك — محفوظ». لا وعد جلسة منفصلة، لا خيار «خطة كاملة» مكرّر. |
| `27-active-workout-modal.png` | سطح التمرين النشط (modal focus، `role=dialog aria-modal`). |

## مطابقة معايير DONE
- **P1-A وعد صادق:** الهيرو يفتح خطة اليوم الحقيقية؛ «١٥ دقيقة» توجيه للتخفيف (أول جزء من الخطة) لا جلسة مولّدة منفصلة. حُذفت بطاقة «خطة كاملة» (كانت تفتح نفس المسار = وعد كاذب). صفر وعد كاذب. (`todayV2Model.ts`)
- **P1-B عزل الخلفية:** سطح التمرين النشط `role="dialog" aria-modal="true"`؛ يبثّ `qimmah:immersive`، و`MobileShell` يجعل الهيدر + شريط التنقل `inert` + `aria-hidden` أثناء التمرين — لا يصلهما VoiceOver. لا يكسر الإنهاء المؤكَّد ولا حارس الثيم.
- **P1-C أهداف اللمس ≥44:** زر العلامة + مبدّل اللغة (`min-h-[44px]`)، زر إغلاق التمرين (`h-11 w-11`)، زر «تعلّم» (`min-h-[44px]`)، وأزرار الأقراص/الإحماء (منطقة لمس موسّعة `before:-inset` تصل ٤٤ دون تغيير الحجم البصري).
- **P1-D تباين الأخضر AA:** `--v2-green-text: #0f7a40` = **5.41:1** على الأبيض (محسوب في `test:polish2`)؛ النص الأخضر الصغير (شارة التقدّم «X من ٤»، نسبة حلقة التغذية) صار يستخدم هذا التوكن. + تجاوز داكن `#3ddc84` للسطح الداكن.

## التغطية
- `test:polish2` (١٤ فحصًا): تباين الأخضر مُحتسَب ≥4.5 · سطح modal + inert · أهداف اللمس ≥44 · وعد العودة صادق.
- `test:today-v2` (٤٥) حُدِّث لنبرة العودة الصادقة الجديدة. `finish-confirm` 11/0 و`theme` 26/0 خضراء بعد تعديل سطح التمرين.
